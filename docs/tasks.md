# 実装タスク

**上から順に着手できる順序で並べてある。** 各タスクは1回のコミットで終わる粒度を目安とし、検証可能な完了条件を持つ。IDは振らない（`D-019`）。

現在の対象は `P-0` のみ。`P-1` 以降は着手する時点で洗い出す。

## この一覧に無いもの

| 対象 | 理由 |
|---|---|
| `019-incremental-update.md` / `020-migration.md` の実装 | **仕様書がまだ無い。** 書いてから分解する |
| 20年分の全レース取得 | `P-0` の完了条件は2〜3年分（`architecture.md` 7節）。全期間は `P-1` 以降 |
| 特徴量・モデル・バックテスト | `P-1` 以降 |

---

## P-0 データ基盤

**完了条件**: 2〜3年分を取り込んで品質検査の `fail` が0件（`architecture.md` 7節 / `D-040`）。

- [ ] uv でプロジェクトを初期化する（`D-042`）
      完了条件: `uv sync` が成功し `uv run python -V` が 3.12 以上を返す。`pyproject.toml` と `uv.lock` がコミットされ、依存に duckdb と polars が含まれる

- [ ] 中間スキーマのDDLを実装する（`001-schema.md` / `R-011` `R-012`）
      完了条件: 7テーブルが作成順（`source_ids` → `horses` → `races` → `runners` → `payouts` → `odds` → `laps`）で作成でき、`001-schema.md` の「テスト観点」6件が通る

- [ ] Fetcher を実装する（`002-loader.md` / `R-016` `R-017`）
      完了条件: 同一URLの2回目が `from_cache=True` を返しHTTPを発行しない。リクエスト間隔の下限を既定5秒より短く設定できない。`robots.txt` が取得を禁止していれば中断する。生HTMLが gzip で永続保存される

- [ ] 生データがコミットされないことをCIで検査する（`R-017` / `D-014`）
      完了条件: 生HTML・キャッシュ・DBファイルをステージした状態でCIが失敗する

- [ ] `fetch_log` と `rejected_rows` を実装する（`002-loader.md` / `R-015`）
      完了条件: 「未取得（行が無い）」と「`outcome='empty'`」が区別できる。`page_kind` と `outcome` の定義外の値がCHECKで拒否される

- [ ] `day_index` のパースを実装する（`002-loader.md`）
      完了条件: 保存済みキャッシュの `20190616` から `race_id` が列挙でき、場コード `02` `05` `09` を含む

- [ ] `archive` の着順テーブルをパースする（`002-loader.md` / `R-013`）
      完了条件: `002-loader.md` の単体テスト1〜6が通る（`4(降)`→降着、`中`→競走中止、`除`→競走除外、未知マーカーは棄却して `rejected_rows` に1件、`計不`→`NULL`、`490(-2)`→`490`/`-2`）

- [ ] `archive` の払戻・ラップ・コーナー通過順をパースする（`002-loader.md` / `D-036`）
      完了条件: 単体テスト7〜10bが通る（`1:08.0`→`68.0`、`---`→`NULL`、コーナー数0で `corners=[]`、コーナー数4で通過欄が空なら `NULL`、要素数不一致は行を棄却）。`combination` が `D-036` の正規形になる

- [ ] エンティティ同定を実装する（`002-loader.md` / `D-035` `D-038`）
      完了条件: 単体テスト11が通る（同じ `source_key` で2回呼ぶと同一の内部IDが返り、`source_ids` は1行のまま）

- [ ] 中間スキーマへの書き込みを実装する（`001-schema.md` `002-loader.md` / `R-012`）
      完了条件: キャッシュ済み6レースが全テーブルに入り、`source` と `fetched_at` が全行で埋まる。`200505050810` と `200605050810` が `archive` のみで揃う

- [ ] テスト用の最小 fixture を作る（`R-017`）
      完了条件: **生HTMLをコミットせずに**回帰テストが動く。`202007010811`（降着）・`202009020204`（同着）・`202304020211`（直線競走）・`201405010303`（不良オッズ）を含む

- [ ] 取り込みの冪等性を検証する（`R-021`）
      完了条件: 同じキャッシュから2回取り込んだ結果が、`fetched_at` を除いて完全一致する

- [ ] `quality_runs` と `quality_findings` を実装する（`012-data-quality.md` / `D-041`）
      完了条件: 両テーブルが作成でき、`severity` の定義外の値が拒否される

- [ ] `fail` 検査7件を実装する（`012-data-quality.md` / `R-014`）
      完了条件: 正常データで7件すべて0件。`012-data-quality.md` の「テスト観点」に挙げた壊れたデータを各検査が検出する。`finish_pos_rank` は同着6ケース（`1,2,2,4` と `1,1,3` は通過、`1,2,2,3` と `1,1,2` と `1,2,4` は検出）で期待どおりになる

- [ ] `warn` 検査4件を実装する（`012-data-quality.md` / `D-040`）
      完了条件: 4件が率と**年代別内訳**を返す。`odds_coverage` が券種別に出る

- [ ] 品質レポートと終了コードを実装する（`012-data-quality.md` / `D-040`）
      完了条件: `fail` が0件なら終了コード0、1件以上なら非0。Markdownレポートに `fail` の違反件数と先頭20件の `race_id`、`warn` の率と年代別内訳が出る

- [ ] **2〜3年分を取り込み、`fail` が0件であることを確認する**（`architecture.md` 7節 / `R-013` `R-014`）
      完了条件: `fail` が0件（`D-040`）。`quality_runs` に実行範囲と件数が記録される。**これが `P-0` の完了**

- [ ] `shutuba` のパースを実装する（`002-loader.md` / `D-037`）
      完了条件: 発走前のレースで出走馬・枠順・馬体重が取れる。**`P-0` の完了条件には含まれない**（当日予測は `P-5`）

- [ ] 出走取消と失格の着順マーカーを対応表に追加する（`002-loader.md` / `D-034`）
      **ブロック中: `Q-023`**（両者の表記を実データで一度も観測できていない）
      完了条件: 実例のレースで `status` が `出走取消` / `失格` に写り、`rejected_rows` に落ちない

- [ ] `weather_forecast` の取得を実装する（`002-loader.md` / `D-029`）
      **ブロック中: `Q-021`**（天気予報の取得元が未定。過去分は原理的に取得できない）
      完了条件: 暫定予測の実行時点で `races.weather_forecast` が埋まる
