# 実装タスク

**上から順に着手できる順序で並べてある。** 各タスクは1回のコミットで終わる粒度を目安とし、検証可能な完了条件を持つ。IDは振らない（`D-019`）。

現在の対象は `P-0`〜`P-3`。`P-4` 以降は着手する時点で洗い出す。

## この一覧に無いもの

| 対象 | 理由 |
|---|---|
| `019-incremental-update.md` / `020-migration.md` の実装 | **仕様書がまだ無い。** 書いてから分解する |
| ~~馬場差推定（`F-301` / `F-302` の本体）~~ | **一覧に入れた**（2026-08-25、`D-103`）。`R-023` 改善の本線と判定されたため「P-3 続き」に置いた。`013-track-variant.md` は未作成のままだが、`Q-025` のデータ前提確認は完了している |
| 20年分の全レース取得 | 10年分（2015-2024）への拡張を実行中（`Q-033`）。20年への追加拡張は `P-3` の結果を見て判断する |
| `202304020211` / `202009020204` の取得 | `Q-026`。取得は `D-014` の条件下で行う必要があり、判断を挟む |
| 確率展開（`008`）・期待値と賭け金（`009`）・バックテスト（`010`） | **仕様書がまだ無い。** かつ `architecture.md` 7節で `P-4` |

---

## P-0 データ基盤

**完了条件**: 2〜3年分を取り込んで品質検査の `fail` が0件（`architecture.md` 7節 / `D-040`）。

- [x] uv でプロジェクトを初期化する（`D-042`）
      完了条件: `uv sync` が成功し `uv run python -V` が 3.12 以上を返す。`pyproject.toml` と `uv.lock` がコミットされ、依存に duckdb と polars が含まれる

- [x] 中間スキーマのDDLを実装する（`001-schema.md` / `R-011` `R-012`）
      完了条件: 7テーブルが作成順（`source_ids` → `horses` → `races` → `runners` → `payouts` → `odds` → `laps`）で作成でき、`001-schema.md` の「テスト観点」8件が通る。`races.corner_nos` が `[]` と `NULL` を区別して保持できる（`D-043`）

- [x] Fetcher を実装する（`002-loader.md` / `R-016` `R-017`）
      完了条件: 同一URLの2回目が `from_cache=True` を返しHTTPを発行しない。リクエスト間隔の下限を既定5秒より短く設定できない。**`db.netkeiba.com` と `race.netkeiba.com` の両方**の `robots.txt` を確認し、いずれかが禁止していれば中断する（`D-014` 条件3）。生HTMLが gzip で永続保存される

- [x] 生データがコミットされないことをCIで検査する（`R-017` / `D-014`）
      完了条件: 生HTML・キャッシュ・DBファイルをステージした状態でCIが失敗する

- [x] `fetch_log` と `rejected_rows` を実装する（`002-loader.md` / `R-015`）
      完了条件: 「未取得（行が無い）」と「`outcome='empty'`」が区別できる。`page_kind` と `outcome` の定義外の値がCHECKで拒否される。**同じURLを2回記録しても1行のまま**で `outcome` と `fetched_at` が2回目の値に更新される（`D-045`、単体テスト13）

- [x] `day_index` のパースを実装する（`002-loader.md`）
      完了条件: 保存済みキャッシュの `20190616` から `race_id` が列挙でき、場コード `02` `05` `09` を含む

- [x] `archive` の着順テーブルをパースする（`002-loader.md` / `R-013`）
      完了条件: `002-loader.md` の単体テスト1〜6が通る（`4(降)`→降着、`中`→競走中止、`除`→競走除外、未知マーカーは棄却して `rejected_rows` に1件、`計不`→`NULL`、`490(-2)`→`490`/`-2`）

- [x] `archive` の払戻・ラップ・コーナー通過順をパースする（`002-loader.md` / `D-036`）
      完了条件: 単体テスト7〜10eが通る（`1:08.0`→`68.0`、`---`→`NULL`、コーナー表0行で `corner_nos=[]`/`corners=[]`、見出し `3コーナー` `4コーナー` で `corner_nos=[3,4]`、通過欄が空なら `corners IS NULL`、要素数不一致は `corners IS NULL` で**行は取り込む**）。`combination` が `D-036` の正規形になる

- [x] エンティティ同定を実装する（`002-loader.md` / `D-035` `D-038`）
      完了条件: 単体テスト11が通る（同じ `source_key` で2回呼ぶと同一の内部IDが返り、`source_ids` は1行のまま）

- [x] 中間スキーマへの書き込みを実装する（`001-schema.md` `002-loader.md` / `R-012`）
      完了条件: キャッシュ済み6レースが全テーブルに入り、中間スキーマの全行で `source` と `fetched_at` が埋まる（`R-012` / `D-046`）。`200505050810` と `200605050810` が `archive` のみで揃う

- [x] テスト用の最小 fixture を作る（`R-017`）
      完了条件: **生HTMLをコミットせずに**回帰テストが動く。`202007010811`（降着）・`201405010303`（不良オッズ）を含む。`202009020204`（同着）と `202304020211`（直線競走）は**ページ未取得のため合成データで代替する**（`Q-026`）

- [x] 取り込みの冪等性を検証する（`R-021`）
      完了条件: 同じキャッシュから2回取り込んだ結果が、`fetched_at` を除いて完全一致する。`fetch_log` と `rejected_rows` の行数も増えない（`D-045`、単体テスト14）

- [x] `quality_runs` と `quality_findings` を実装する（`012-data-quality.md` / `D-041`）
      完了条件: 両テーブルが作成でき、`severity` の定義外の値が拒否される

- [x] `fail` 検査9件を実装する（`012-data-quality.md` / `R-014`）
      完了条件: 正常データで9件すべて0件。`R-014` の8項目と `check_id` の対応表（`012-data-quality.md`）に**どちらの側にも余りが無い**。`012-data-quality.md` の「テスト観点」に挙げた壊れたデータを各検査が検出する。`finish_pos_rank` は同着6ケース（`1,2,2,4` と `1,1,3` は通過、`1,2,2,3` と `1,1,2` と `1,2,4` は検出）で期待どおりになる

- [x] `warn` 検査5件を実装する（`012-data-quality.md` / `D-040`）
      完了条件: 5件が率と**年代別内訳**を返す。`odds_coverage` が券種別に出る。`corners_missing` が完走馬のみを分母にする（`D-044`）

- [x] 品質レポートと終了コードを実装する（`012-data-quality.md` / `D-040`）
      完了条件: `fail` が0件なら終了コード0、1件以上なら非0。Markdownレポートに `fail` の違反件数と先頭20件の `race_id`、`warn` の率と年代別内訳が出る

- [x] **2〜3年分を取り込み、`fail` が0件であることを確認する**（`architecture.md` 7節 / `R-013` `R-014`）
      完了条件: `fail` が0件（`D-040`）。`quality_runs` に実行範囲と件数が記録される。**これが `P-0` の完了**
      完了: 2022-01-05〜2024-12-28（9,985レース・137,795出走行）を取り込み、`fail` 検査9件すべて0件を確認した（`run_id=2`）。取り込み中に距離表記の穴を2件発見・修正（`外-内`・`内2周`）。`D-048` で出走取消の着順マーカーを確定し、既存データもバックフィル済み。残る `warn` は一過性の `http_error` 2件のみ

- [ ] `shutuba` のパースを実装する（`002-loader.md` / `D-037`）
      完了条件: 発走前のレースで出走馬・枠順・馬体重が取れる。**`P-0` の完了条件には含まれない**（当日予測は `P-5`）

- [x] 失格の着順マーカーを対応表に追加する（`002-loader.md` / `D-034`）
      完了条件: 実例のレースで `status` が `失格` に写り、`rejected_rows` に落ちない
      **出走取消（`取`）は `D-048` で解決済み**（220件確認、バックフィル済み）
      完了: 10年分拡張取り込み中に実例を1件発見（`202005050104`、着順欄表記は `失` の1文字）。`_STATUS_MAP` に追加し再取り込みで解決した。`Q-023` を解決した

- [ ] `weather_forecast` の取得を実装する（`002-loader.md` / `D-029`）
      **ブロック中: `Q-021`**（天気予報の取得元が未定。過去分は原理的に取得できない）
      完了条件: 暫定予測の実行時点で `races.weather_forecast` が埋まる

---

## P-1 特徴量パイプライン + リーク検査テスト

**完了条件**: `004-leakage-test.md` の検査9件が通る（`architecture.md` 7節「発走時点情報のみをテストで保証」）。

**順序の要点**: リーク検査の骨組みを特徴量の実装より**前**に通す。後から入れると、それまでに書いた特徴量をすべて見直すことになる。骨組みが通った後は、特徴量を1つ足すごとに検査が緑のままであることを確認する。

### 土台

- [x] `FeatureSpec` と `FeatureRegistry` を実装する（`003-features.md` / `R-028`）
      完了条件: 全 `F-xxx` が `timing` と `race_level` を持って登録され、属性を欠いたまま登録できない。`columns_for("暫定")` が `timing='当日'` の列を含まない（`D-028`）
      完了: `src/umagic/features/registry.py`。`Q-019`（発走何分前か未確認）の特徴量は本命経路からも除外する既定動作にした

- [x] `build_features` の骨格を実装する（`003-features.md` / `004-leakage-test.md`）
      完了条件: `build_features(conn, as_of=D)` が `(race_id, horse_id)` で一意な `pl.DataFrame` を返す。特徴量列は0本でよい。同じ入力で2回呼んで**ビット完全一致**する（`D-055`）
      完了: `src/umagic/features/build.py`。`race_ids` 未指定時は `date < as_of` のレースが対象

- [x] 集計順序の決定性を実装する（`003-features.md` / `D-055`）
      完了条件: 集約前に `(race_id, horse_id)` で整列している。行の投入順を変えても出力がビット一致する
      完了: 基底集合を結合前後の両方で整列する

### リーク検査の骨組み（特徴量より先）

- [x] 合成 fixture を作る（`004-leakage-test.md` / `D-053`）
      完了条件: `004` の「合成 fixture が持つべき性質」5項目をすべて含む（同日・同競馬場の連続R番号／同日・複数競馬場／同一馬の時系列複数出走／少走馬と多走馬の混在／期間をまたぐレース）。生HTMLを含まない（`R-017`）
      **規模は `Q-028` で未決。** まず最小構成で作り、実行時間を測ってから調整する
      完了: `tests/fixtures/leakage_fixture.py`。11レース・15出走。5性質すべてを満たす最小構成。実行時間は現時点で問題にならない規模（テスト全体で2秒台）

- [x] リーク検査9件の骨組みを実装する（`004-leakage-test.md` / `R-020`）
      完了条件: `tests/test_leakage.py` に9件の `test_` 関数が存在し、特徴量が0本の状態で全件通る。CIで `-m "not realdata"` で実行される
      完了: 当初案（`feature_fns=[]` で自明に通す）より強い形にした。各原則を体現する最小 probe 関数を実装し、「正しい実装で通る」ことを実際に検証する。`feature_fns=[]` の自明ケースは `tests/test_features_build.py` で別途確認済み

- [x] 欠陥注入テストを実装する（`004-leakage-test.md`）
      完了条件: `004` の「テスト観点」10件それぞれで、欠陥を仕込むと該当検査が**落ちる**ことを確認する。欠陥を仕込まない状態では全件通る
      **これが無いと、検査が素通りしていても気づけない**
      完了: 9/10件を実装（`tests/test_leakage.py` 冒頭の対応表）。**#8（Stage 1 を全期間で学習）は原理的に今は書けない。** Stage 1 本体が `P-3` まで存在せずSQL probeで代替できないため。`test_stage1_fault_injection_deferred` として明示的に `skip` し、`006-stage1-pace.md` 実装時に本物へ差し替える

- [x] 実データ版のリーク検査を実装する（`004-leakage-test.md` / `D-053`）
      完了条件: `tests/test_leakage_realdata.py` が `pytest -m realdata` でのみ実行され、CIから除外される。`data/umagic.duckdb` に対して9件が通る
      完了: 血統取得（`D-050`、21,189/21,189頭）完了後、`pytest -m realdata` で9件全て確認した

### 横断規約（`003-features.md` 共通規約）

- [x] `F-902` 縮約を実装する（`003-features.md` / `D-051`）
      完了条件: `θ = (n·x̄ + k·μ_global)/(n+k)` が `n=1` で `μ_global` に強く寄り、`n=100` でほぼ `x̄` になる。**`μ_global` が `as_of` までのデータのみで計算される**（`R-019`）
      完了: `src/umagic/features/shrinkage.py`。`per_row_stat_before` は対象行ごとにその行自身の日付で母集団を切る（`D-054` の追記事項）

- [x] `F-901` レース内相対化を実装する（`003-features.md` / `D-021`）
      完了条件: `race_level=False` の特徴量が生値・`_z`・`_rank` の3列を出す。`race_level=True` の特徴量には `_z` `_rank` の列が**存在しない**。`race_std=0` のとき `_z` が `NaN`。分母は `n_starters`（`D-012`）
      完了: `src/umagic/features/relative.py`。`race_level=True` への不適用は呼び出し側の責務（`relativize` 自体は `race_level` を知らない）

- [x] 欠損と指示子列を実装する（`003-features.md` / `D-058`）
      完了条件: 補完せず `NaN` を返す。構造的欠損で `<feature>_unavailable=1`、取得漏れで `0` になる。両者が区別できる
      完了: `src/umagic/features/missing.py`。「構造的」の判定式は特徴量ごとに異なるため呼び出し側が渡す

- [x] 全履歴と直近N走の窓を実装する（`003-features.md` / `D-059`）
      完了条件: 集計系の特徴量が `_all` と `_recent` の両方を出す。`N` が設定値として外から与えられる
      完了: `src/umagic/features/window.py`。`n_all` / `n_recent` も返し `F-902` にそのまま渡せる

### 特徴量（1つ足すごとにリーク検査が緑のままであること）

- [x] `F-101` 逃げ意欲を実装する（`003-features.md` / `D-043`）
      完了条件: `003` のテスト観点1〜4が通る。**`corner_nos` に `1` を含む過去走のみを使い、3角・4角で代用しない**。1角の無いレースで `f101_unavailable=1`
      完了: `src/umagic/features/f101.py`。「過去走はあるが1角を含む走が無い」（構造的）と「過去走が無い」を区別する。`D-044` により完走しなかった馬（`corners IS NULL`）は履歴として使えないことも確認した

- [x] `F-103` ペース適性を実装する（`003-features.md` / `D-061`）
      完了条件: `003` のテスト観点14が通る（`p_i` に**自馬が含まれない**）。`F-101` が欠損するレースで `F-103` も `NaN` になる
      完了: `src/umagic/features/f103.py`。着差の数値化テーブル（`D-064`）と `p_i` の集約式（`D-065`、他馬F-101平均の反転）を新規に決定してから実装した。`μ_global` は対象行ごとの日付未満の累積平均（`D-054`）。`p_i` の分散が0（回帰不能）のケースも構造的欠損として扱う

- [x] `F-104` 展開交互作用を実装する（`003-features.md` / `D-021`）
      完了条件: `003` のテスト観点7が通る（`F-102` が 2.7 と 40.0 で `F-104` が異なる＝潰れていない）。`F-103` の側だけを相対化してから積を取る
      **`F-102` は Stage 1（`P-3`）の出力なので、`P-1` の時点では `F-104` も `NaN` になる**
      完了: `src/umagic/features/f104.py`。`build_features` の `feature_fns` には登録せず、`f102`/`f103`/`n_starters` を既に持つ `DataFrame` を受け取る純粋関数にした（`F-102` が存在しない `P-1` 時点では呼び出し側が列を用意できないため、`FeatureFn` として組み込む意味が無い）。テスト観点7はF-102を直接注入する合成データで検証

- [x] `F-303` 上がり3F関連を実装する（`003-features.md`）
      完了条件: `last_3f` の縮約平均と過去走でのレース内上がり順位が出る。`F-301` に依存しない
      完了: `src/umagic/features/f303.py`。`last3f_all`/`last3f_recent`（`D-051`/`D-059`）と `last3f_rank_in_race`（縮約なし、単純平均）を実装。`window.py` の `full_and_recent` を複合キー（`[race_id, horse_id]`）対応に拡張した（同じ馬でも対象行ごとに履歴が異なるため）。「直近N走」は `last_3f` が記録されている過去走に限る（`F-101` の既存の扱いに合わせた）

- [x] `F-302` の接続点を定義する（`003-features.md` / `D-060`）
      完了条件: `attach_f302(df, horse_effects)` の型が定義され、`horse_effects` が空のとき列が `NaN`・指示子 `1` になる。**簡易版のフォールバックを実装しない**
      **`013-track-variant.md` が未作成のため、列は `NaN` のまま**（`Q-025`）
      完了: `src/umagic/features/f302.py`。`(horse_id, as_of)` の完全一致で仮結合する（`013` 自体が未実装のため、asof結合が要るかなどの精緻な結合方法は未定・`Q-025`）

- [x] `F-201` / `F-202` 血統を実装する（`003-features.md` / `D-062`）
      完了条件: `003` のテスト観点13が通る（`sire_id IS NULL` で `NaN`・指示子 `1`）。条件別成績が `F-902` で縮約される。**embedding を作らない**
      完了: `src/umagic/features/f201.py`（ID passthrough）・`f202.py`（種牡馬/母父の条件別成績）。条件 `c` の距離帯は `D-066`（4区分）、馬場状態を含めないのは `D-068`（`F-2xx` の締切=木曜と `track_condition` が当日まで不明という制約の衝突）。「成績」の定義（`finish_pos/n_starters`）は `D-067`。`μ_global,c` は条件バケツ×血統IDごとの日付順累積を `join_asof(by=...)` で対象行ごとに引く（`F-103` と同じ手法の一般化）
      **`F-203`（条件変化適性）は数式が未確定のため未実装（`Q-030`）**

- [x] `F-501` 当日の脚質バイアスを実装する（`003-features.md` / `D-010`）
      完了条件: `003` のテスト観点8〜11が通る（同日・同競馬場・R番号が小さいレースのみ／別競馬場は含まない／同一R番号は含まない／1Rで `NaN`・指示子 `1`）
      **同日レースを参照してよいのはこの2つだけ**
      完了: `src/umagic/features/f501.py`。同日・同競馬場・同馬場（芝/ダート）・`race_number` が対象より小さいレースの上位3着馬の4角通過順比率を平均する
      **`F-502` は「クラス別基準タイム」が未定義のため未実装（`Q-031`）。`F-302`（`013` 待ち、`D-060`）と同じ概念に依存する**

- [x] `F-503` 開催週次を実装する（`003-features.md` / `D-063`）
      完了条件: `meeting_no` と `meeting_day` から構成される。**柵移動の列を持たない**（`Q-027`）
      完了: `src/umagic/features/f503.py`

- [x] `F-6xx` 反動・ローテーション・馬体重を実装する（`003-features.md`）
      完了条件: 前走が無い馬で `NaN`・指示子 `1`。`status='降着'` の走を除外できる（`D-034`）。`F-603` の `timing` が `当日`
      完了: `src/umagic/features/f601.py`（前走着順・着差・上がり3F順位。`exclude_relegated` で降着走を除外できる）・`f602.py`（中週数・前走グレード。放牧明け閾値は仕様が与えないため実装しない）・`f603.py`（馬体重・増減）
      **`F-601` の「高強度指標」（人気ベース）は `D-002` の過去人気利用範囲との整合が未確定のため未実装（`Q-032`）**

- [x] `F-7xx` 人的要因を実装する（`003-features.md` / `D-002` `Q-006`）
      完了条件: `F-701` が**過去レースの**人気で交絡を除去し、**対象レースの `odds_win` / `popularity` を参照しない**（`R-018`）。`F-703` が `affiliation` から遠征を判定する（`D-049`）
      完了: `src/umagic/features/f701.py`（人気帯=人気値そのものでの残差、二重のas-of累積）・`f702.py`（乗り替わり・騎乗経験回数）・`f703.py`（遠征フラグは `D-069` の対応表、主戦騎手フラグ）・`f704.py`（`F-202` と同型、`jockey_id`×コース・距離帯）

- [x] `F-8xx` 基礎・コースを実装する（`003-features.md` / `D-028`）
      完了条件: `F-803` に天候・馬場状態が**含まれない**（`F-804` に分離）。`F-804` の `timing` が `当日`。`track_condition` が順序尺度に写る。`F-801` の分母が `n_starters`（`D-012`）
      完了: `src/umagic/features/f801.py`（条件バケツ `(course,distance_band,n_starters,frame)`、`D-070`）・`f802.py`（`F-202`/`F-704`と同型、馬自身×コース・距離帯）・`f803.py`（季節は月から機械的に切る）・`f804.py`（`weather`/`weather_forecast`/`track_condition` を別列で出す。**`weather_forecast` は木曜、他は当日と `timing` が本来異なるが、`FeatureSpec` は1特徴量1`timing` の制約があり、レジストリへの実登録は未整理のまま残す（どの `F-xxx` もまだ実登録していないため `P-1` をブロックしない）**

### 完了確認

- [x] **`P-1` の完了を確認する**（`architecture.md` 7節 / `R-019` `R-020` `R-028`）
      完了条件: リーク検査9件と欠陥注入テスト10件が通る。実データ版（`pytest -m realdata`）も通る。**これが `P-1` の完了**
      **`F-102` `F-104` `F-302` は `NaN` のまま**（Stage 1 は `P-3`、`F-301` は `Q-025` 待ち）。`D-054` が要求する「Stage 1 も as-of 検査の対象」は `P-3` で実効になる
      完了: `uv run pytest tests/ -q` で248件通過・1件skip（`test_stage1_fault_injection_deferred`、`P-3` でStage 1実装時に本物へ差し替え）。リーク検査9件・欠陥注入9/10件（合成データ）・実データ版リーク検査9件（`data/umagic.duckdb`、9,985レース・137,795出走行・血統21,189/21,189頭）すべて通過。**`P-1` 完了**。個別特徴量では `F-203`（`Q-030`）・`F-502`（`Q-031`）・`F-601` の高強度指標（`Q-032`）が未実装のまま残るが、いずれも他の特徴量をブロックしない

---

## P-2 ベースライン

`005-baseline.md` が唯一の仕様書（Draft）。学習を伴わないため `003-features.md` にも `014-training-pipeline.md`（未作成）にも依存しない（`D-075`）。

- [x] 対象母集団の抽出を実装する（`005-baseline.md` 1節 / `D-071` `D-076`）
      完了条件: `population='all'`/`'g1'` それぞれで対象レースが取れる。封印セット（`D-017`、G1のみ）が両方の母集団から除外され、開封回数には計上されない（`D-076`）。封印期間内の**非G1**レースは除外されない
      完了: `src/umagic/baseline.py` の `target_races()`

- [x] 市場確率の確率指標を実装する（`005-baseline.md` 2節）
      完了条件: `005` のテスト観点1・2・4が通る（正規化の和が1、1着同着のラベル等分、`競走中止`馬が分母に含まれる）。`出走取消`/`競走除外`馬が正規化の分母に含まれない（テスト観点3）
      完了: `probability_metrics()`。`status NOT IN ('出走取消','競走除外') AND odds_win IS NOT NULL` で対象を絞る

- [x] ベタ買い戦略の回収率を実装する（`005-baseline.md` 4節 / `D-072` `D-073` `D-077`）
      完了条件: `005` のテスト観点5〜7が通る（`favorite`が`popularity=1`を選ぶ、複勝払戻2行のレースで3着を的中扱いしない、`payouts`に無い組み合わせが例外を出さず払戻0円になる）。的中判定は`payouts`の存在で行い、着順から複勝・ワイドの成立条件を再実装しない（`D-072`）
      完了: `race_ledger()`。`comb_key`（`"番号"` or `"小-大"`）で `payouts` を引く。回収率の点推定はレース横断で `payout_yen`/`stake_yen` の合計比を取ればよく、`ReturnMetrics`（CI付き）の組み立ては次のブートストラップのタスクで行う

- [x] ブートストラップ信頼区間を実装する（`005-baseline.md` 5節 / `D-078` `R-025`）
      完了条件: `005` のテスト観点10・13が通る（同じ`seed`でビット完全一致、1レースのみでも例外を出さない）。リサンプリング単位がレースである（`D-078`）
      完了: `bootstrap_roi_ci()` と、`race_ledger()`と組み合わせて`ReturnMetrics`を作る`return_metrics()`。**実装中にバグを発見・修正**: 当初 `seed+i` で毎回シードを進めていたが、隣接するシード値（`seed=1`と`seed=2`）は`polars`の`sample()`でほぼ同じ抽出列を生成し、パーセンタイル境界が偶然一致していた（`test_different_seed_can_differ`で発覚）。`random.Random(seed)`を経由して副シードを生成する方式に修正した

- [x] 年別内訳とレポート出力を実装する（`005-baseline.md` 3節・6節 / `D-018` `R-027`）
      完了条件: `005` のテスト観点14が通る（`by_era`の`n_races`合計が母集団全体と一致する）。`BaselineReport.to_markdown()` が信頼区間・検出力の注記（`g1`母集団）・封印除外件数を含む
      完了: `by_era_breakdown()`（点推定のみ。年×母集団×戦略×券種の全組み合わせで10,000回のブートストラップを回すと実行時間が過大になるため、CIはトップレベルの`ReturnMetrics`のみが持つ）・`run_baseline()`・`BaselineReport.to_markdown()`

### 完了確認

- [x] **`P-2` の完了を確認する**（`architecture.md` 7節 / `R-023`）
      完了条件: `run_baseline()` を実データ（`data/umagic.duckdb`）に対して実行し、基準線のLogLossと回収率が出る。`005` のテスト観点11・12（`uniform`単勝roiが控除率20%の理論値≈0.80付近、`favorite`単勝roiが0.75〜0.80）で実装の検算が取れる
      **`g1`母集団の回収率は開発用36レースで検出力が無い（`Q-033`）。参考値として報告し、有意な結論を出さない**
      完了: `uv run pytest tests/ -q -m "not realdata"` で264件通過・1件skip。実データ（9,951レース、封印36件を除外）で `run_baseline()` を実行し、`all`母集団: LogLoss 1.9058・`favorite`単勝roi **0.800**（控除率20%の理論値と一致、実装の検算成功）。`g1`母集団（36レース）は`favorite`単勝roi 0.475・95%CI [0.194, 0.792] と幅が大きく、`Q-033`どおり検出力が無いことを実測でも確認した。
      **テスト観点11の予想が誤りだったと判明（`005-baseline.md` に記録済み）**: `uniform`単勝roiは0.705で控除率理論値(0.80)より明確に低い。人気別に単勝roiを独立集計すると1番人気0.800→18番人気0.541と概ね単調に下がる（favorite-longshot bias）。`baseline.py`を経由しない生SQLでの独立検算で一致を確認し、実装のバグではないと確認した
      **`P-2` 完了**

---

## P-3 Stage 1 + Stage 2 + walk-forward検証

**完了条件**: 開発用検証のG1で LogLoss が市場確率を下回る（`architecture.md` 7節 / `R-023` = A判定）。

**着手前に外すべきブロッカーが2つある。** `Q-033`（データ量）は `P-3` 全体を止め、`Q-035` は `D-087` の前提そのもの。いずれもこの一覧の先頭2タスクで扱う。

**順序の要点**: `014`（fold生成・`sample_weight`）を `006`/`007` より前に置く。fold が決まっていないとモデルを評価できない。`015`（校正）は `007` の出力を入力に取るので最後。

### 着手ブロッカーの解消

- [x] 端数距離レースのラップ本数を実データで確認する（`006-stage1-pace.md` / `Q-035`）
      完了条件: 有馬記念（2500m）など200mの倍数でない距離のレースで、`laps` の本数と `走破タイム ≒ Σ lap_sec` を照合し、**端数が先頭に来るか末尾に来るかを確定する**。末尾に来る場合は `D-087` の「末尾3本＝600m」という前提が崩れるため、`spec-hearing` に戻って式を見直す
      **`D-087`（Stage 1 の目的変数）の前提そのもの。** 実装より先に潰す
      完了: キャッシュ済みHTML 12,678レースページ（21種類の距離）を走査。**本数は `ceil(距離/200)`、端数は先頭**で末尾3本は常に600m。`D-087` の前提は成立した。ただし**当初の式に端数距離での偏り**（前半の除数を区間数にしていたため2500mで約0.6秒ハイペース側に振れる）を発見し、除数を実距離ベースに修正した（`D-087` の追記、`Q-035` は Resolved）

- [x] 10年分の取り込みを完了し、`fail` が0件であることを確認する（`012-data-quality.md` / `Q-033`）
      完了条件: 2015-2024の全レースが取り込まれ、品質検査の `fail` が0件。血統取得も完了する。`014` の fold が**7本**生成できる（検証2018〜2024。ただし2024の検証期間はG1が全て封印内で0件、2023は約16競走のみ。`D-080`）
      完了: レース取り込み完了（33,236レース・467,927出走行）。取り込み中に `payout_horses` で8件の違反を検出し、失格マーカー未対応（`Q-023`）が原因と判明・解決した（`D-034` 追記）
      完了: 血統取得完了（53,856頭中53,712頭・99.7%。`http_error` 144件は `fetch_log` に未完了として残るため再実行で自動的に拾い直せる。残り0.27%は `D-058` の欠損規約で正常系として扱われるため P-3 の着手は妨げない）
      完了: `run_quality_checks()` を10年分の全データ（33,236レース・467,927出走行）に対して再実行し、`fail` 9項目すべて0件を確認した（`Q-033` Resolved）

- [x] `lightgbm` を依存に追加する（`D-042`）
      完了条件: `uv add lightgbm` が成功し、`uv.lock` がコミットされる。`uv run python -c "import lightgbm"` が通る。**`D-042` はスタックとして選定済みだが `pyproject.toml` に入っていない**
      完了: `lightgbm==4.7.0`（`numpy==2.5.2` を同時に導入）。`group` を渡した `lambdarank` の最小学習が通ることを確認した。**`scipy==1.18.0` が `lightgbm` の推移的依存として入った**が `pyproject.toml` には宣言していない（`D-097` は `015` の温度推定を自前実装と定めており、直接使わない）

### 学習パイプライン（`014-training-pipeline.md`）

- [x] fold 生成を実装する（`014-training-pipeline.md` / `D-079` `D-080` `R-022`）
      完了条件: `014` のテスト観点1〜8が通る（fold数7・`train_end < valid_start`・`train_years` で expanding/sliding が切り替わる・封印G1が学習と検証の両方から除かれる・**封印期間の非G1は学習に現れる**・同じ `seed` で fold の `seed` が一致・`seed+i` のような連番にならない・`min_train_years<3` が `ValueError`）
      完了: `src/umagic/training.py` の `Fold`/`make_folds()`。3年分のデータでは `Q-033` どおり fold が0本になることもテストで固定した

- [x] `sample_weight` を実装する（`014-training-pipeline.md` / `D-081` `D-003`）
      完了条件: `014` のテスト観点9・10が通る（クラス別重みが適用され、辞書に無いクラスが `1.0` にフォールバックする）。**JpnIのキーは無いまま**（`Q-034`）
      完了: `sample_weights()`。`"JpnI"` を仮のクラス名として渡し、辞書に無くても例外にならず `1.0` になることを確認した

- [x] クロスフィッティングの分割を実装する（`014-training-pipeline.md` / `D-086`）
      完了条件: `014` のテスト観点11が通る（`n_blocks` 個のブロックが時系列順に並び、重複せず学習期間を覆う）。**ランダム分割にしない**（`D-054` / 原則7）
      完了: `cross_fit_blocks()`。日数が `n_blocks` に満たない場合は `ValueError`（仕様に無い防御的な追加）

- [x] モデルの保存・読み込みを実装する（`014-training-pipeline.md` / `D-082` `R-021`）
      完了条件: `014` のテスト観点12・13が通る（`meta.json` の `feature_names` が順序込みで一致し、列順が違うと `ValueError`）。`git rev-parse HEAD` と `uv.lock` のSHA-256が記録される
      完了: `save_model()`/`load_model()`/`verify_feature_order()`/`git_commit()`/`uv_lock_sha256()`。**`verify_feature_order()` は仕様の関数一覧に無かった追加**（「読み込み時に入力の列順と突き合わせる」という仕様文を満たすには、`load_model()` 単体は入力列を知らないため別関数が要ると判断した）。`save_model()` は `git_commit`/`uv_lock_sha256` を自動注入しない純粋なシリアライザにし、値の注入は呼び出し側（`006`/`007`）の責務にした

### Stage 1（`006-stage1-pace.md`）

- [x] Stage 1 の目的変数を実装する（`006-stage1-pace.md` / `D-087` `D-091`）
      完了条件: `006` のテスト観点1〜7が通る（`[12.0]*10` で `0.0`・前半が速いと正・遅いと負・**ラップ本数12本と13本で1ハロンあたりに揃う**・`laps` が無いレースと3本以下のレースが `build_target()` に現れない・`laps` が無いレースでも推論できる）
      完了: `src/umagic/stage1.py` の `build_target()`。観点4は2400m(12本、一律12.0秒)と2500m(13本、1本目6.0秒＝100m分)が同じペースで`f102_actual=0.0`に揃うことを実測し、`D-087`の除数修正が効いていることを直接確認した

- [x] Stage 1 の入力を実装する（`006-stage1-pace.md` / `D-089`）
      完了条件: `006` のテスト観点8・9・12・13が通る（`F-101` 全馬欠損で `f101_n_missing = n_starters`・**「逃げ馬1頭＋差し17頭」と「先行6頭」で `f101_mean` が同値でも `f101_min`/`f101_q25` が異なる**・`laps` 由来の列を含まない・`odds_win`/`popularity` を含まない）
      完了: `build_inputs()`。`F-101`の集約対象は`D-094`と同じ母集団（出走取消・競走除外を除く）にした

- [x] Stage 1 のモデルを実装する（`006-stage1-pace.md` / `D-088` `D-090`）
      完了条件: `006` のテスト観点10・11・14・15が通る（`track_condition` 欠損で推論できる・同じ `seed` でビット完全一致・fold の学習期間より後のレースが対象に入らない・`meta.json` に `n_excluded_no_laps` が残る）。`Stage1Model` Protocol を満たす LightGBM 回帰の実装が既定
      完了: `LightGBMStage1Model`（`categorical_feature`は自前で整数コード化。`deterministic`/`force_row_wise`/`num_threads=1`で`R-021`を満たす）。**観点14は実装中にテスト設計の誤りを発見した**: `compute_f101`（`003`）は`as_of`引数をSQL内で使わず、各行自身のレース日付が過去走の境界になる。`as_of`を固定して対象レース自身の日付を変える形に直し、境界が正しく機能することを確認した

### Stage 2（`007-stage2-ranker.md`）

- [x] ラベルと学習母集団を実装する（`007-stage2-ranker.md` / `D-093` `D-094`）
      完了条件: `007` のテスト観点3〜7が通る（`1,2,3,4着`→`3,2,1,0`・1着同着2頭が両方`3`・**`競走中止` が `label=0` で学習に含まれる**・`出走取消` が学習と推論のどちらにも現れない・`group` の合計が学習行数と一致）
      完了: `src/umagic/stage2.py` の `build_labels()`/`race_group()`

- [x] カテゴリの丸めを実装する（`007-stage2-ranker.md` / `D-092` `R-019`）
      完了条件: `007` のテスト観点8〜10が通る（`min_count` 未満が `other_code` に落ちる・検証期間にのみ現れるカテゴリが `other_code` に落ちて例外にならない・**丸め表の作成が検証期間の行を数えていない**）
      完了: `build_category_mappings()`/`apply_category_mappings()`。**`null`（`sire_id`欠損など）は`other_code`に丸めずnullのまま残す**（`D-058`。LightGBMのネイティブ欠損分岐に委ねる。仕様の表には明記されていなかった判断）

- [x] Stage 2 の学習と出力を実装する（`007-stage2-ranker.md` / `D-095` `R-002` `R-003`）
      完了条件: `007` のテスト観点1・2・11・12・14・15が通る（`win_prob` のレース内合計が `1.0±1e-6`・**全特徴量が `NaN` の馬にも確率が付く**・全欠損列が `feature_names` に残る・同じ `seed` でビット完全一致・`meta.json` に `inner_logloss` と `inner_ndcg3` の両方・同レースの全出走行が同じ `sample_weight`）
      完了: `fit_stage2()`/`predict_win_prob()`/`race_metrics()`。`lambdarank`のearly stoppingにレース内softmax後のLogLossを使うカスタム`feval`を実装（`first_metric_only=True`で選択指標に）。`deterministic`/`force_row_wise`/`num_threads=1`で`R-021`を満たす。**`predict_win_prob()`に`horse_id`引数を追加した**（仕様の型シグネチャに無いが、出力が`horse_id`列を持つと仕様書自身が書いており、`x`/`race_id`だけでは復元できないため）。**観点12・13（全欠損列が`feature_names`に残る／`build_features()`の出力に`odds_win`等が無い）は`003-features.md`の`build_features()`側（入力特徴量の組み立て）の責務とし、本タスクの範囲外にした**（`007`自身の型シグネチャに組み立て関数が無く、既存の`P-1`テストが別途保証する）

### 確率校正（`015-calibration.md`）

- [x] 温度スケーリングを実装する（`015-calibration.md` / `D-097` `R-002` `R-021`）
      完了条件: `015` のテスト観点1〜7・10・11が通る（合計 `1.0±1e-6`・`T=1.0` で素の softmax と一致・`T>1` で均一方向／`T<1` で尖る方向・**順位が変わらない**・校正済み人工データで `T*≈1.0`・尖った人工データで `T*>1` かつ LogLoss が改善・境界で `at_bound`・同じ入力でビット完全一致）。**`scipy` を追加しない**
      完了: `src/umagic/calibration.py`。最適化は自前の黄金分割探索（`_golden_section_minimize`）。**テスト実装中に `numpy.bool`（NumPy 2.x の新スカラー型）が `polars` のスキーマ推論で `Float64` に化けることを発見した**（`h == winner` の比較結果を `bool()` で明示変換せずにDataFrame化すると型が壊れる）。テストデータ生成箇所を修正して回避した
      完了: 校正済み人工データ（真の確率どおりに勝者を抽選）で `T*` が2000レースで1.04に収束することを実測。過度に尖った人工データ（真のlogitを5倍）では `T*≈5.9` となり `logloss_after<logloss_before` を確認した

- [x] 校正データの作成と適用範囲を実装する（`015-calibration.md` / `D-098` `D-099`）
      完了条件: `015` のテスト観点8・9・12・13が通る（1着同着のラベル等分・G1が0件の学習期間で `T=1.0` かつ例外にしない・G1に絞る契約がテストで固定される・**`all` 母集団に校正が適用されていない**）
      完了: `fit_calibrator()`。**観点13（`all`母集団への不適用）は本モジュールの範囲外にした**（`007`の観点12・13と同じ理由。`Calibrator`は母集団の区別を一切持たず、「`all`には`.apply()`を呼ばない」という判断はorchestration層の責務）

### walk-forward の実行

- [x] walk-forward の実行を実装する（`014-training-pipeline.md` / `D-083` `R-022`）
      完了条件: `014` のテスト観点14が通る（`fold_index` が全 fold ぶん含まれ、`(race_id, horse_id)` が fold をまたいで重複しない）。**指標は計算せず**、全 fold の予測を積んだ `DataFrame` を返す（`D-083`）
      完了: `src/umagic/training.py` の `run_walk_forward()`。**`predict_fold(conn, fold) -> DataFrame` を呼び出し側から差し替え可能な依存として受け取る設計にした**（`014` 自身の6節が `run_walk_forward(...)` のパラメータを明示しておらず、Stage 1 のクロスフィッティング・Stage 2 の学習・確率校正を1 foldについてどう束ねるかを指定していないため）。fold をまたぐ `(race_id, horse_id)` の重複と、`predict_fold` の戻り値の列欠落をどちらも `ValueError` にした（仕様に無い防御的な追加）

- [x] `predict_fold` の結線（orchestration層）を実装する（`006` `007` `014` `015` / `D-100`）
      完了条件: `003-features.md` の全特徴量（`F-203`/`F-502` 除く。未実装のため）+ `F-102`/`F-104`/`F-302` を組み立て、Stage 1 クロスフィッティング（`D-086`）→ Stage 2 学習・校正データ作成（`D-098`）→ `run_walk_forward()` 経由の検証期間予測、まで例外なく通しで走る。合成データ（複数年・複数頭・血統・騎手/調教師の重複履歴）でのスモークテストで確認する
      完了: `src/umagic/orchestration.py`。`Stage2FoldRunner.predict_fold()` が本体。クロスフィット用サブモデルの inner 検証省略と、校正を `run_walk_forward()` の外で適用する設計は `D-100` に記録。`class_weights`/`min_count` の暫定値は `D-101`。`stage2._CATEGORY_COLS` を血統ID以外のカテゴリ列（`f602_prev_grade`/`f803_*`/`f804_*`）まで拡張した（実際に全特徴量を組み立てて初めて判明した、文字列型のまま渡すと `lightgbm` がエラーになる列）。`R-023` 判定用の最小実装として `run_p3_completion_check()` も置いた（`010-backtest.md` 未作成のためのつなぎ）
      完了: `tests/test_orchestration.py`（6件）・`tests/fixtures/orchestration_fixture.py`。合成データでの通しテストの過程で `races.grade`（文字列）が誤って特徴量行列に混入する実装ミスを発見・修正した
      完了: `tests/test_leakage.py` の欠陥注入 #8（「Stage 1 を全期間で学習」）を実装した。`006-stage1-pace.md` 実装時から skip のまま残っていたテスト観点。`umagic.stage1` を直接使い、学習期間に含まれない未来レース（distance=3600m という他に存在しない値と、極端な `f102_actual` を持たせる）を学習データに含めた場合と含めない場合で、同じ distance のクエリへの予測が変わることを確認する
      **コミット後にセルフレビュー（`code-review` スキル）を実施し、3件の実バグを発見・修正した**（`D-100` に追記）:
      (1) fold の日付範囲から `race_id` を確定する箇所が `is_sealed()` を呼んでおらず、`today` に近い fold で封印G1が学習・検証の両方に混入していた（`D-017`/`D-079` 違反）。`_race_ids_in_range()` を新設して修正
      (2) `apply_g1_calibration()` が、校正前提の生スコアではなく既に softmax 済みの `y_pred` を `Calibrator.apply()` に渡しており、二重に softmax を取っていた（校正が数学的に誤った値になる）。`Stage2FoldRunner.fold_valid_scores` に生スコアを保持し直す形に修正
      (3) 「`early_stopping_rounds` を大きくすれば早期終了が起きない」という前提が誤りだった（LightGBM は最終ラウンドで無条件にロールバックする）。早期終了の仕組み自体を使わない `stage2.fit_stage2_fixed_rounds()` を新設して修正（実測: 30ラウンド要求して1〜7ラウンドしか残らない状態だった）
      あわせて重複コード（`_train_race_ids_all`/`_valid_race_ids_all` の統合、`stage2.build_category_mappings()` への `columns` 引数追加による重複実装の削除）と、`f103_z`/`f103_rank` の二重relativize（実装ミス）も修正した。テスト `tests/test_orchestration.py` を6件→8件に拡充（バグ回帰テストを追加）
      **性能改善**: レビューで指摘された最も重い非効率（Stage 1 の入力組み立て `F-101` の相関自己結合を含む高コストなSQLを、クロスフィットの `n_blocks` ブロックごとに再実行していた）を修正。`stage1_build_target()`/`stage1_build_inputs()` は対象レース自身の過去走だけを見る行ごとに独立な計算のため、学習期間全体で1回だけ呼びブロックにはスライスして与える設計（`stage1_fit_all()`。旧 `stage1_oof_and_full()`/`stage1_fit_full()` を統合）に変更し、fold あたり `n_blocks+1` 回だったこの重いクエリを1回に削減した。あわせて `_race_ids_by_date()` の重複クエリも1箇所削減

- [x] `class_weights`/カテゴリ丸め閾値のハイパーパラメータ探索を実装する（`D-081` `D-092` `D-051` `D-059` / `D-101`）
      完了条件: fold ごとの inner 検証（`D-084`）で複数候補を比較し、`class_weights`・`D-092` の `min_count` の既定値を決める。決めた値と根拠を `D-101` に追記する
      完了（2026-08-24）: 実データの fold 0・fold 6（inner LogLoss平均、G1に限らない全レース）で12候補（`class_weights`4種 × `min_count`3種）を比較。一度は最良候補（`class_weights`強め）を採用したが、**7fold walk-forward の実測でG1のモデルLogLossが悪化した**（`2.2985→2.4030`）ため、**探索前の暫定値 `{"G1":5,"G2":3,"G3":2,"L":1.5}` に戻した**。`min_count=20` は探索・実測のどちらでも問題なく採用。詳細と教訓（探索指標をG1に絞るべきだった）は `D-101` に記録
      **`F-902` の `k`・`F-101`等の窓 `N` は今回のスコープ外。** 各特徴量モジュールの関数デフォルトに埋め込まれており、`FEATURE_FNS` を候補ごとに差し替える追加の配線が要るため、別タスクとして残す（下記）

- [ ] `F-902` の `k`（縮約強度）・`F-101` 等の窓 `N` のハイパーパラメータ探索を実装する（`D-051` `D-059` / `D-101`）
      完了条件: `FEATURE_FNS` の各特徴量関数を候補ごとのパラメータで差し替え可能にし（`functools.partial` 等）、**G1のOOFスコア（`D-098`と同じクロスフィッティング）のLogLossで**複数候補を比較する（学習期間全体のinner LogLossでは`class_weights`探索と同じ轍を踏む。`D-101`の教訓）。決めた値と根拠を `D-051`/`D-059` に追記する
      **優先度: 低に変更（2026-08-25、`D-103`）。** 要因診断の結果、`R-023` に対してほぼ無価値であることが実測で示された。fold 0 のグループ別アブレーションでは `F-902` の縮約を使う `F-202`(−0.0068)・`F-802`(−0.0059)・`F-103`(−0.0040) がいずれも**外した方が良い**側に並んでおり、縮約強度の調整で動く範囲を超えている。削除はしない（いずれ値を決める必要はある）が、`Q-025`（`F-301`/`F-302`）と `Q-039`（生IDの締め出し）の後に回す
      **旧記述**: `R-023` 未達の要因候補として `class_weights`/`min_count` より疑わしい、としていた
      **予備調査（2026-08-24、結論には至らず）**: `F-103`/`F-202`/`F-303`/`F-701`/`F-704`/`F-801`/`F-802` の7特徴量に共有の`k`（`N`は既定のまま固定）で、fold 0（G1 68レース）のOOF LogLossを4候補（2/5/10/20）で比較した。`k=20`が最良（`2.2874`）、`k=10`が最悪（`2.3843`）と**非単調**で、1fold・68レースという小標本のノイズが疑わしい。**この結果だけでは`DEFAULT_K`（現状5.0）を変更しない。** 複数foldでの再検証が必要（`class_weights`探索の反省を踏まえ、単一fold・単一指標の結果を軽々しく採用しない）

- [ ] `class_weights` をG1指標で再探索する（`D-081` / `D-101`）
      完了条件: 学習期間全体のinner LogLossではなく、`D-098`と同じクロスフィッティングで作ったG1のOOFスコアのLogLossを選定指標にして `class_weights` の候補を比較し直す
      **背景: 1回目の探索（上記）は選定指標を誤り、実際にはG1性能を悪化させる候補を最良と誤判定した。** 同じ轍を踏まないよう、選定指標を変えてやり直す必要がある
      **優先度: 低に変更（2026-08-25、`D-103`）。** 要因診断により、`R-023` 未達の主因がハイパーパラメータではないことが実測で示された。とくに診断2（市場確率との対数線形ブレンドで最良重みが `w_model = 0.00`、23,224レース）は、**現在のモデルが市場に対して上乗せ情報を持たない**ことを十分な検出力で示しており、`class_weights` の再調整が動かせる範囲（`D-101` の実測で 0.10 程度）ではギャップ（0.28〜0.41）に届かない。削除はしないが `Q-025` / `Q-039` の後に回す
      **なお選定指標の作り方（`D-098` と同じクロスフィッティングによるG1 OOF）は `Q-039` の比較で実装済み**（`data/diag8_g1_oof.py`）。再開するときはそれを流用できる

- [x] `Q-038`（`orchestration.py` の再現性）を調査・修正する（`R-021`）
      **優先度: 高。他のP-3実測タスクの数値の信頼性に関わる。**
      完了条件: `assemble_stage2_matrix()`/`predict_fold()` 内の全 `join` を洗い出し、`race_group()`/LightGBM学習に渡す直前で `(race_id, horse_id)` 順に整列されていない箇所を特定・修正する。同一入力を3回以上実行し `y_pred` がビット完全に一致することをテストで固定する
      完了（2026-08-24）: **原因は `join` の行順ではなく `F-103`（`_ols_slope()`）の数値不安定性だった。** `p_i` の分散が極小な馬で `beta=cov_xy/var_x` が悪条件になり、DuckDBの並列集計に由来する無害な浮動小数点誤差（`~1e-14`）が大きく増幅されていた。`beta` は全馬共通の `mu_global` に混ざるため、少数の不安定な `beta` の誤差が全行の約60%に伝播していた。`_ols_slope()` に分散下限ガード（`MIN_VAR_X=1e-6`）を追加し、実行間の `f103` 最大差を `1.21e-7`→`3.15e-11`（約3800分の1）に縮小した（`D-102`）
      **完全なビット完全一致は達成していない**（DB接続のシングルスレッド化が必要だが費用対効果が見合わず見送った）。`R-021` は「通常の浮動小数点丸め誤差（`~1e-11`以下）を除く範囲」で満たすものとして扱う（`D-102` に明記）
      **この修正より前に得た `R-023`・年代別内訳の数値（上記）は不安定だった `f103` を経由しているため、参考値。** 確定させるには修正後の再実行が必要（未実施、次のタスクとして残す）

- [x] `D-102`（`F-103` 修正）後に `R-023`・年代別内訳を実データで再実行する
      完了条件: `run_p3_completion_check()` を実データ7foldで再実行し、`docs/tasks.md` の「`P-3` の完了を確認する」の数値を修正後の値に更新する（参考値の注記を外す）
      完了（2026-08-24）: 確定版はG1 LogLoss 2.3015・市場1.8912（約21.7%劣後、修正前の2回の値2.2985/2.3270の間に収まった）。年代別内訳も含め上記「`P-3`の完了を確認する」に反映済み

- [x] `Q-036` を実データで確認する（`007-stage2-ranker.md` / `Q-036`）
      完了条件: 実データで Stage 2 を学習し、`softmax(score)` のレース内最大値とエントロピーを、`005-baseline.md` の市場確率の同じ統計量と比較する。**飽和または均一化が確認されたら `D-095` を見直す**
      完了（2026-08-24）: `all`母集団23,224レースで比較。model（未校正）は市場より max_p が低く entropy が高い＝**むしろ市場より控えめ**で、飽和は起きていない。`D-095` の見直しは不要（`Q-036` Resolved、詳細は `open-questions.md`）

- [x] `Q-037` を実データで確認する（`015-calibration.md` / `Q-037`）
      完了条件: 頭数帯（`≤12` / `13〜15` / `16〜18`）ごとに `T` を別々に推定し、単一 `T` との LogLoss の差を比較する。**差が大きければ `D-097` を頭数依存の形に差し替える**
      完了（2026-08-24）: G1のOOFスコア（7fold分プール、203レース）で比較。頭数帯別Tと単一Tの差は最大でも約0.5%（`16-18`頭・171レースでは0.01%）で、**単一Tで十分**と判断。`D-097` の見直しは不要（`Q-037` Resolved、詳細は `open-questions.md`）

### 完了確認

- [x] **`P-3` の完了を確認する**（`architecture.md` 7節 / `R-023`）
      完了条件: 開発用検証のG1で、モデルの LogLoss が `005-baseline.md` の市場確率の LogLoss を**下回る**（`R-023` = A判定）。比較は `D-075` に従い**同じレース集合**で行う。`D-018` / `R-027` の年代別成績を併記する
      **封印セットを開かない。** A判定は開発用検証で行う（`D-015`）
      **A判定に届かなくても `P-3` の実装は完了しうる。** `D-015` は「Aのみの達成も成果」としており、未達は `D-001` が承知していた難度の帰結。その場合も数値を記録して次の判断材料にする

      **実測（2026-08-24、`D-102`〔`F-103`の数値安定性修正〕後の確定版）**: `run_p3_completion_check()` を実データ7fold（`2015-2024`、封印年除く）で実行した。所要時間 約101分。**`Q-038`/`D-102`修正前の2回の実行値（`2.2985`・`2.3270`）はいずれも不安定な`f103`を経由した参考値。以下が確定版。**

      | 母集団 | レース数 | モデル LogLoss | 市場 LogLoss | A判定 |
      |---|---|---|---|---|
      | `g1`（校正後） | 135 | 2.3015 | 1.8912 | **未達**（市場を約21.7%上回る＝悪い） |
      | `all`（校正前） | 23,224 | 2.2209 | 1.9365 | 未達（参考値） |

      **年代別内訳（`D-018`/`R-027`。fold=検証年）**:

      | fold | 年 | all: n | all model | all market | all差 | g1: n | g1 model | g1 market | g1差 |
      |---|---|---|---|---|---|---|---|---|---|
      | 0 | 2018 | 3,328 | 2.2300 | 1.9334 | +0.2967 | 27 | 2.5430 | 1.9562 | +0.5868 |
      | 1 | 2019 | 3,324 | 2.2485 | 1.9636 | +0.2850 | 24 | 2.2948 | 1.9302 | +0.3646 |
      | 2 | 2020 | 3,295 | 2.2516 | 1.9919 | +0.2596 | 24 | 1.8603 | 1.4734 | +0.3869 |
      | 3 | 2021 | 3,326 | 2.2259 | 1.9496 | +0.2763 | 24 | 2.2236 | 1.8804 | +0.3431 |
      | 4 | 2022 | 3,331 | 2.2129 | 1.8986 | +0.3144 | 24 | 2.6372 | 2.2458 | +0.3913 |
      | 5 | 2023 | 3,317 | 2.2090 | 1.9326 | +0.2764 | 12 | 2.1388 | 1.8153 | +0.3235 |
      | 6 | 2024 | 3,303 | 2.1685 | 1.8861 | +0.2824 | 0 | 封印で0件 | — | — |

      **`all`母集団の市場比の差は年によらずほぼ一定（+0.26〜+0.31）。** これは特定の年のデータ品質問題ではなく、**モデル全体に構造的・一様なギャップがある**ことを示唆する（`D-001` が最初から織り込んでいたG1〔および全レース〕市場の効率性そのものである可能性が高い）。G1側は年ごとの標本が12〜27件と小さく振れは大きいが、**全ての年で市場を下回ったことは一度も無い**。`D-102`修正前の数値と比べても年代別の傾向は変わらない（`all`差の平均は修正前後どちらも約0.28。`F-103`の数値不安定性は`all`のような大標本では平均化されて目立たなかった）

      **未達だが `P-3` の実装自体は完了。** `D-015` どおり数値を記録した。残る要因候補は `class_weights` のG1指標での再探索・`F-902`/`F-101` のハイパーパラメータ（いずれも上記タスク）だが、年代別内訳が示す一様なギャップを踏まえると、**パラメータ調整だけでは埋まらず、特徴量そのものの予測力（またはG1市場の効率性の限界）を疑うべき**局面に来ている。

      **要因診断の結果（2026-08-25、`D-103`）**: 上の見立てを実データで検証し、裏付けが取れた。新規の学習をほとんど伴わない診断で、以下が確定した。

      | # | 判明したこと | 標本 |
      |---|---|---|
      | 1 | ギャップの正体は識別力。最適温度を当てても縮むのはギャップの**1.6%**のみ | 23,224R |
      | 2 | **モデルは市場に上乗せ情報を持たない**（対数線形ブレンドの最良 `w_model = 0.00`） | 23,224R |
      | 3 | 確率の目盛りは壊れていない（信頼性曲線の実測/予測が0.87〜1.07、本命の自信/実力 0.945） | 23,224R |
      | 4 | 学習不足でもない（`best_iteration` は fold 0 で64・fold 1 で65、上限200） | fold 0/1 |
      | 5 | **生ID 4本が gain の69〜74%を占め、設計特徴量を締め出している**（未使用列 67〜71/110） | fold 0/1/2 |
      | 6 | **能力の絶対値（走破時計ベース）に相当する特徴量が1本も無い**。`F-302` は全欠損 | — |

      2 が最も重い。23,224レースという十分な検出力で「市場確率を持っているならモデルの出力を混ぜる価値がゼロ」と出ている。**この状態で `P-4`（期待値・ケリー）に進めば控除率20%ぶんそのまま負ける**ため、`P-4` の着手はブロックされる。

      5 は `Q-039` として登録した。安く効く（`all` で −0.031〜−0.039、3 fold で再現・有意）が、**G1では逆を向いており採用保留**（`D-101` と同じ構造）。単独では市場とのギャップの約14%にとどまる。

      6 が本線。`Q-025` のデータ前提は確認済みで（`time_sec` カバー率100%、5,764セッションが単一連結成分）、残るのは実装方法の選択のみ。

---

## P-3 続き（要因診断を受けた作業。`D-103`）

- [x] `Q-039` の候補をG1 OOF指標で比較する（`D-103` / `Q-039`）
      完了条件: `D-098` と同じクロスフィッティングで作った**学習期間のG1 OOF**（fold 0 で68・fold 1 で95・fold 2 で119レース）で、基準・ID系4本除去・`cat_smooth`/`cat_l2` の各候補を比較する。fold の学習期間は入れ子なので**プールせず fold ごとに独立して見て、向きが一致するかを確認する**
      完了（2026-08-25）: **ID系4本除去を棄却した。** `all`（9,947R）では3 fold とも有意に改善（−0.0313）していたが、G1 OOF では3 fold とも悪化（+0.0905 / +0.0807 / +0.0402）。血統・騎手のIDはG1では実際に予測力を持っており、問題はIDそのものではなく正則化の不足だった。`all` を選定指標にしていれば `D-101` と同じ誤りを繰り返していた
      残る候補は `cat_smooth=200`（単独）と `cat_smooth=200 + cat_l2=200`（併用）の2案のみ。**`all` と G1 OOF の両方で・3 fold すべて改善側に揃った唯一の設定群**（併用案の G1 OOF 3fold平均 −0.0736、`all` −0.0390）。詳細は `Q-039`

- [ ] `cat_smooth=200 + cat_l2=200` を7 fold walk-forward で確認する（`Q-039` / `D-101` / `R-023`）
      完了条件: この設定を既定にして `run_p3_completion_check()` を実データ7 fold で実行し（約101分）、**`R-023` の判定指標そのもの（検証G1 135レース）** で基準（現状 2.3015）と比較する。改善すれば `D-xxx` を起こして `src/umagic/stage2.py` の `full_params` に反映し、悪化すれば `D-101` と同じくG1実測を優先して棄却する
      **G1 OOF の18比較のうち個別に有意だったのは1つだけ**（`cat_smooth=200`・fold 1）で、根拠は「向きが3/3で一致すること」に依存している。向きの一致だけなら偶然でも1/8で起こるため、判定指標そのもので確認するまで採用しない

- [ ] `Q-040`（`F-102` が Stage 2 に寄与していない）を切り分ける（`D-007` / `Q-040`）
      完了条件: Stage 1 自身の予測精度（`f102_actual` に対する `f102` の `R²` または RMSE）をラップのあるレースで測り、「Stage 1 が当たっていない」のか「当たっているが Stage 2 の役に立たない」のかを判定する。結果を `Q-040` に記録する
      **`D-007` はリーク回避のための構造でもあるため、「Stage 1 をやめて実測ラップを使う」という選択肢は無い。** 廃止するなら `F-102`/`F-104` ごと落とすことになる

- [ ] `min_data_in_leaf` の根拠を確定するか既定に戻す（`D-103`）
      完了条件: `src/umagic/stage1.py` / `src/umagic/stage2.py` が LightGBM 既定（20）ではなく `1` を使っている理由を `D-xxx` として記録するか、既定に戻す。**性能には効いていない**ことは実測済み（fold 0 で 20/50/100 に変えても有意差なし）だが、`docs/` に根拠が無い設定が残っている

### `Q-025` の解消（`R-023` 改善の本線。`D-103`）

- [x] `013-track-variant.md` を作成する（`Q-025` / `F-301` / `F-302`）
      完了条件: `spec-hearing` で混合効果モデルの実装方法を決め、仕様書を `Draft` で作る
      完了（2026-08-25）: `docs/spec/013-track-variant.md`。実装方法は**自前の交互反復**（`D-104`。`MixedLM` は交差ランダム効果を33,236+53,712水準で扱えず、`PyMC` は最低35回のMCMCが現実的でないため脱落）。目的変数は `(surface, distance)` ごとに標準化（`D-105`。レース内偏差のSDが芝1200m 0.809秒〜ダート2400m 2.216秒と2.7倍違うため）。as-of 粒度は `D-086` の4ブロックを流用（`D-106`）。`F-302` は馬効果のみ・as-of 結合（`D-107`）。`Q-025` を解決した
      **識別可能性の確認でカタログとの差が判明**: レース単位の二部グラフは単一連結成分ではなく18成分（最大98.07%）。外れた625レースは全て2024-06以降の新馬・未勝利で、**データ窓の末尾で必ず起きる境界の産物**だった。仕様の5節で最大成分外を `NaN` + 指示子として扱う規定を置いた

- [x] `F-301` / `F-302` を実装する（`013-track-variant.md` / `D-104`〜`D-107`）
      完了条件: `013-track-variant.md` の「テスト観点」18件が通る。`f302` が全欠損でなくなり、リーク検査9件が緑のまま（実データ版も含む）。`D-054` の原則7（推定期間も as-of で切る）を満たす
      **`D-060` により簡易版のフォールバックを実装しない**
      完了（2026-08-25）: `src/umagic/track_variant.py`（`fit_track_variant()`/`horse_effect_series()`。固定効果は `numpy.linalg.pinv` を1回だけ計算し反復ごとの再フィットは行列積のみにして高速化）。`src/umagic/features/f302.py` を as-of 結合に変更（`D-107`）。`src/umagic/orchestration.py` に `track_variant_fit_all()` を新設（`stage1_fit_all()` と対称。fold あたり5回、`D-106`）し `assemble_stage2_matrix()`/`predict_fold()` に結線。`tests/test_track_variant.py`（21件、仕様書テスト観点18件に対応）・`tests/test_features_f302.py`（4件）を追加。`pytest tests/ -q -m "not realdata"` 392件・実データ版リーク検査9件、いずれも通過
      **実データで2件発見・`D-108` に記録**: (1) 実データ規模（14万〜42万行）では反復が5000回でも収束フラグが立たない。ただし100回時点との差は実測で最大 `2.5e-4`（効果の標準差 `0.99` に対して無視できる）ため、収束判定を相対誤差に変更したうえで `DEFAULT_MAX_ITER=100` を維持した。(2) 出走1回しかない馬が極端な走破タイムを引くと縮約が弱く効き、効果が二桁に達することがある（実例: 芝1600mを149.5秒。ロバスト化は未対応、`Q-041`/`Q-042` と合わせて `R-023` 実測後に判断）
      **性能**: 3年分（143,656行）1.2秒、9年分（419,328行）3.6秒。fold あたり5回×7 foldでも `P-3` の総所要時間（約101分）への追加は数分程度の見込み

- [x] `F-302` を入れて7 fold walk-forward を再実行し `R-023` を測り直す（`v5`）
      完了（2026-08-25）: `data/p3_full_run_v5.py`。**悪化した**（G1 model 2.3015→2.3460、all model 2.2209→2.2904。市場は不変）。`all` は7fold全てで悪化（+0.048〜+0.080、v3比）。fold 0 のアブレーションで `f302` 系列（3列）を除くと検証LogLossが0.0507改善することを確認し、`f302_rank` が理論値を超える異常値（1.25、本来1.0が上限）を持つことから `D-109` のバグを発見した

- [x] `build_features()` の基底集合バグを修正する（`D-109`）
      完了条件: `status IN ('出走','降着','競走中止','失格')` に絞り、`build_labels()` と同じ条件に揃える。実データで `_rank` 列が1.0を超えないことを確認する
      完了（2026-08-25）: `src/umagic/features/build.py`。回帰テスト追加（`tests/test_features_build.py`）。`pytest tests/ -q -m "not realdata"` 372件・実データ版9件、いずれも通過。実データで `f202_sire_rank` 等が正しく1.0以下に収まることを直接確認した
      **影響範囲は`F-301`/`F-302`に限らない。** 確認しただけで21列（`P-1`実装済みの既存特徴量）に同じ超過があった（`D-109`）。33,236レース中1,611件（4.8%）が影響対象

- [x] バグ修正のみで7 fold walk-forward を再実行し `R-023` を測り直す（`v6`）
      完了（2026-08-25）: `data/p3_full_run_v6.py`（`include_track_variant=False`）。G1 model 2.3015→2.3355（+0.034）、all model 2.2209→2.2194（−0.0015）。`all`は全fold±0.005以内でほぼ不変。**これが `v3` の数値の「本当の」基準**。G1は3勝3敗で符号が混在（135レースの標本ノイズ、悪化ではない）

- [x] バグ修正後の基準で `F-302` の効果を測り直す（`v7`）
      完了（2026-08-25）: `data/p3_full_run_v7.py`（`include_track_variant=True`）。`v6`比で G1 model 2.3355→2.3839（+0.0484）、all model 2.2194→2.2896（+0.0702）。**`all`は7fold中7fold、+0.0475〜+0.0869の狭い幅で一貫して悪化**（`v3→v5`の悪化とほぼ同じ大きさ・同じ一貫性で再現）。`D-109`のバグでは説明できず、`F-302`自体が有害と確定した（`D-110`）
      完了条件どおり `Q-041` の判断材料になった: **補正タイム集計を足す前に、`F-302`（馬効果のみ）自体の問題を解く必要がある**

- [x] `Stage2FoldRunner.include_track_variant` の既定値を決める（`D-110`）
      完了（2026-08-25）: 既定を `False` に変更。`F-302`は実装として残るが、明示的に有効化しない限りStage 2に組み込まれない。回帰テスト更新（`tests/test_orchestration.py`）。`pytest tests/ -q -m "not realdata"` 373件・実データ版9件、いずれも通過

- [ ] `F-302` がなぜ有害かを診断する（`D-110` / `Q-042`）
      完了条件: 原因の仮説を1つ以上検証する。候補: (a) `Q-042`（レース効果がメンバーレベルを吸っており、馬効果側にその残差が漏れている） (b) 縮約の分散基準（`k_horse = σ²_error/σ²_horse`）が外れ値に弱い（`D-108`で見つけたが`|f302_z|>5`の馬は0頭で直接の裏付けは取れていない） (c) 他の未検証要因
      **`Q-041`（集計系を足すか）はこれを解いてから判断する。** 壊れている土台に足しても悪化が拡大するだけの可能性が高い
