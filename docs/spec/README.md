# 実装仕様書

**このディレクトリはまだ空である。** 実装に着手する段階で仕様書を追加する。

## 位置づけ

| ドキュメント | 書くこと | 書かないこと |
|---|---|---|
| `decisions.md` | なぜそうするか | 実装の詳細 |
| `domain-knowledge.md` | 何を根拠にするか | 実装の詳細 |
| **`spec/`** | **何をどう作るか** | **判断の根拠**（`D-xxx` を参照するに留める） |

仕様書に根拠を書き込まないこと。根拠は `decisions.md` に `D-xxx` として置き、仕様書からは参照する。
こうしておくと、仕様が変わっても根拠が失われず、根拠が変わったときに影響範囲を追える。

## ファイル命名

```
docs/spec/NNN-<名前>.md
```

`NNN` は3桁の連番。Phase順に振ることを想定する。

想定される仕様書（現時点の見込み。確定ではない）:

| ファイル | 対応Phase | 内容 |
|---|---|---|
| `001-schema.md` | P-0 | 中間スキーマの型定義、制約、正規化ルール |
| `002-loader.md` | P-0 | ソース抽象インターフェース、各ローダーの責務 |
| `003-features.md` | P-1 | 特徴量の実装仕様（`F-xxx` ごと） |
| `004-leakage-test.md` | P-1 | リーク検査テストの仕様 |
| `005-baseline.md` | P-2 | ベースラインモデルの定義 |
| `006-stage1-pace.md` | P-3 | レース質予測モデル |
| `007-stage2-ranker.md` | P-3 | 適性照合モデル |
| `008-probability.md` | P-4 | Plackett-Luce展開 |
| `009-betting.md` | P-4 | 期待値計算と賭け金決定 |
| `010-backtest.md` | P-4 | 検証手順とレポート様式 |
| `011-cli.md` | P-5 | CLIのコマンド体系 |

## テンプレート

```markdown
# NNN <タイトル>

| | |
|---|---|
| Phase | P-x |
| 関連決定 | D-xxx, D-xxx |
| 関連特徴量 | F-xxx, F-xxx |
| 状態 | Draft / Fixed / Implemented |

## 目的

このモジュールが何を担うか。1〜3文。

## 入出力

入力と出力の型・形式を明記する。

## 仕様

実装すべき内容。曖昧さが残らない粒度で書く。

## 制約

守るべき条件（リーク防止、性能要件など）。

## テスト観点

何を検証すれば「できた」と言えるか。

## 未決事項

この仕様書の範囲で未確定のもの。`Q-xxx` として `open-questions.md` にも登録する。
```
