# UMAGIC ドキュメント

JRA平地G1に限定した競馬予想モデルの設計ドキュメント群。

## ドキュメントの役割分担

このプロジェクトでは **「なぜそうするか」と「何を作るか」を意図的に分離** している。
設計の根拠を仕様書に混ぜると、仕様変更のたびに根拠が失われ、
逆に根拠を残したまま仕様を書くと読み手が実装すべきものを見失うため。

| ドキュメント | 役割 | 誰が読むか |
|---|---|---|
| [`requirements.md`](./requirements.md) | 満たすべき要件（`R-xxx`） | 完了判定をする人 |
| [`decisions.md`](./decisions.md) | 確定した設計判断とその根拠（ADRログ） | 設計変更を検討する人 |
| [`domain-knowledge.md`](./domain-knowledge.md) | プロの予想プロセスの形式化・特徴量カタログ | 特徴量を実装する人 |
| [`architecture.md`](./architecture.md) | システム構成・スキーマ・Phase計画・評価設計 | 全体像を掴む人 |
| [`open-questions.md`](./open-questions.md) | 未決事項と、決めるために必要な情報 | 次に判断する人 |
| [`spec/`](./spec/) | **実装仕様書（今後追加）** | 実装する人 |
| [`tasks.md`](./tasks.md) | 実作業と完了条件（`P-0`〜`P-3`） | 手を動かす人 |

`design.md` は作らない（`D-019`）。設計は `architecture.md`（全体）と `spec/`（モジュール単位）が担う。

## ID体系

ドキュメント間の相互参照のため、以下のIDを使う。
仕様書を書く際は、これらのIDを参照して「どの決定・どの特徴量の実装仕様か」を明示する。

| プレフィックス | 対象 | 定義場所 |
|---|---|---|
| `D-xxx` | 決定事項（Decision） | `decisions.md` |
| `F-xxx` | 特徴量（Feature） | `domain-knowledge.md` |
| `Q-xxx` | 未決事項（Open Question） | `open-questions.md` |
| `P-x` | 開発フェーズ（Phase） | `architecture.md` |
| `R-xxx` | 要件（Requirement） | `requirements.md` |

`tasks.md` のタスクにはIDを振らない（`D-019`）。

IDは**一度振ったら再利用しない**。決定が覆った場合は元のIDを `Superseded by D-xxx` として残し、新しいIDを振る。

## 仕様書を書くときの手順

1. `docs/spec/` に `NNN-<名前>.md` を追加する（[テンプレート](./spec/README.md)を使う）
2. 冒頭で関連する `D-xxx` / `F-xxx` を参照する
3. 仕様の過程で新たな設計判断が発生したら、`decisions.md` に `D-xxx` を追加してから仕様書側で参照する
   （仕様書の中に根拠を書き込まない）
4. 未決事項が出たら `open-questions.md` に `Q-xxx` を追加する

## 現在の状態

**`P-0`〜`P-3` は完了。`P-4`（Plackett-Luce展開・期待値・ケリー）は着手できない状態にある。**

### Phase の進捗

| Phase | 状態 | 備考 |
|---|---|---|
| `P-0` データ基盤 | **完了** | 2015-2024の10年分。33,236レース・467,927出走行・53,856頭。品質検査の `fail` 9件すべて0件 |
| `P-1` 特徴量 + リーク検査 | **完了** | リーク検査9件・欠陥注入10件が通る。実データ版（`pytest -m realdata`）も通る |
| `P-2` ベースライン | **完了** | `favorite` 単勝roi 0.800 が控除率20%の理論値と一致し、実装の検算が取れた |
| `P-3` Stage 1 + Stage 2 + walk-forward | **実装は完了。`R-023` は未達** | G1でモデルLogLoss 2.3085・市場 1.8912（`D-113`確定後の基準値、約22.1%劣後） |
| `P-4` 期待値・ケリー | **着手不可** | 下記「`P-4` に進めない理由」 |

### ドキュメントの規模

- 設計判断は `decisions.md` に147件（うち2件は Superseded: `D-016` `D-033`）
- 要件は `requirements.md` に30件（`R-001`〜`R-030`）
- 未決事項は `open-questions.md` に44件（Open 24件 / Resolved 20件）
- 実装仕様書は `spec/` に11件（`001`〜`007` / `012` `013` `014` `015`、いずれも Draft）
- 実作業は `tasks.md` に `P-0`〜`P-3`。テストは `tests/` に382件

### `P-4` に進めない理由（`D-103`）

`R-023` 未達の要因を実データで診断した結果（2026-08-25）、**現在のモデルは市場確率に対する上乗せ情報を持たない**ことが分かった。市場確率とモデル確率を対数線形ブレンドして重みを探索すると、23,224レースという十分な検出力で最良重みが `w_model = 0.00` になる。この状態で期待値計算に進めば控除率20%ぶんそのまま負ける。

診断で分かったことは他に4点ある。ギャップの正体は校正ではなく識別力（最適温度で縮むのはギャップの1.6%のみ）。確率の目盛りは壊れていない（信頼性曲線の実測/予測が0.87〜1.07）。学習不足でもない（`best_iteration` は上限200に対して64〜65）。そして**能力の絶対値（走破時計ベース）に相当する特徴量が1本も無い**（→下記 `F-301`/`F-302`）。

### `F-301`/`F-302`（馬場差推定）は実装済みだが既定で無効（`D-104`〜`D-112`）

`Q-025` は解決済み（実装方法は自前の交互反復、`D-104`）。しかし実装して7fold walk-forwardで測定したところ、**`all` 母集団を7fold全て（+0.048〜+0.087の狭い幅）で一貫して悪化させた**。実装過程で `build_features()` の既存バグ（`D-109`、出走取消・競走除外が基底集合に混入し `_rank` 列が1.0を超える。`F-301`/`F-302`に限らず既存21列に影響）も発見・修正したが、修正後の汚染されていない基準でも `F-302` は悪化させた（`D-110`）。

原因究明として `Q-042`（レース効果へのメンバーレベル混入）を検証した。`race_class` が `G1`〜`G3`・`L`・無格付を `'オープン'` 1水準に丸めていることを確認し、`grade` を固定効果に追加した（`D-111`）。7fold walk-forwardで再測定すると **G1側の悪化幅は半減した**（+0.0484→+0.0229）が、**`all`側はほぼ無風**（+0.0702→+0.0697、`D-112`）。`Q-042`はG1側の副次的要因として部分的に実証されたが、`all`が一貫して悪化する主要因ではないと確定した。

`all`側は追加で分解診断も行った（`D-114`）。`F-302`の生値（relativize前）を含む構成は一貫して悪く、除いた構成は一貫して良かった——生値の無制限な尺度（縮約が弱い馬で効果量が二桁に達しうる）が主犯の一つ。しかし relativize 後（`z`のみ・`rank`のみ）でも全除去には届かず、**relativize後もF-302の中身自体がノイズとして働いている**ことが分かった。単純な後処理では埋まらず、`F-301`の推定方法自体（縮約の頑健化等）の見直しが要る、より大きな課題として残った。

`Stage2FoldRunner.include_track_variant` の既定値は `False` のまま。フルの7fold再検証は、見つかったどの候補も現状の既定を上回らなかったため実施していない。

`Q-039`（生IDのカテゴリ正則化）は解決済み。`cat_smooth=200`・`cat_l2=200`を`Stage2FoldRunner`の既定にし、7fold walk-forwardで確認した——`all`が7fold全て（−0.028〜−0.039の狭い幅）・G1（135レース集計）が−0.0270、いずれも改善した（`D-113`）。このセッションで唯一、`R-023`を実際に前進させた変更である。

### 本丸の答え: 現特徴量では `R-023` は達成できない（`D-119`）

`D-103` が残した本丸（モデルが市場に上乗せ情報を持たない）を切り分けた結果、**データ問題であることが確定した。**

市場確率を `init_score`（オフセット）として与え、モデルには**市場からの差分だけ**を学習させた（`f=0` で厳密に市場を再現する構成。`rounds=0` が3 fold とも市場と完全一致することで検算済み）。**市場を渡してもなお、`all` で得られる上乗せは最大 `0.0002`〜`0.0010` で5ラウンドで消え、G1 では一度も市場を下回らない。** `R-023` が埋めるべきG1のギャップ `0.4173` に対し、測定された上限はその **0.1% 以下**である。

順位指標（top-1的中率・NDCG@3）も同じ形で劣化したため、**目的関数の不一致（`lambdarank` は NDCG を最適化する）でも確率の尺度の問題でもない**。取り出す信号が無い。

**この結果は今セッションの経過をすべて説明する。** `F-302` が悪化させ（`D-110`）、`F-103` が不活性で（`D-118`）、`Q-039` の正則化だけが効いた（`D-113`）——いずれも市場が既に織り込んでいる同じ公開情報を再処理していたからである。**特徴量の作り込み・ハイパーパラメータ探索・縮約設計への追加投資は、下記の穴を埋めない限り `R-023` に対して無価値である。**

### 次に手を付けるもの

### 成功定義を改訂した（`D-121` 〜 `D-123`）

複勝プールも測った結果、**券種を変えても市場は超えられない**と確定した（`D-121`）。モデル本命の複勝回収率は市場本命を下回り（`all` で 0.8243 対 0.8386）、**市場に逆らうほど回収率が下がる**（0.8465 → 0.8060 → 0.7821、全バケツ有意に100%割れ）。`D-103` の診断2が複勝の実払戻でも再現した形である。

これを受けて `D-015` の成功定義を改訂した（`D-122`）。

- **`R-023`（旧A判定）・`R-024`（B判定）は削除せず、「現データでは達成不可能」という判定結果を注記して維持する**
- **新A判定 `R-029`: G1の情報捕捉率 `(一様LL − モデルLL) / (一様LL − 市場LL)` が `0.75` 以上**（現状 **0.5468**、上限 `1.0`）
- **`R-030`: `P-4` の着手条件を「市場への上乗せ `w_model > 0` が有意」と明文化**

**A判定の意味が変わったことを隠さない。** `D-015` は A判定に「オッズに乗っていない情報を掴んでいる」という含意を持たせていたが、`D-119` がそれを不可能と実証したため、新A判定は「モデルとして市場に匹敵する水準に達する」という意味になる。

`F-304`（中央値ベースの速度指数）を本番に採用することも決めた（`D-123`）。市場は超えないが `label` との相関 `+0.2345` で**本プロジェクト2番目に強い特徴量**であり、`R-029` を押し上げる主力になる。

### プロジェクトの目的を定め直した（`D-124`）

**当面の目的は「市場が持っていない情報を将来投入するための土台を整えること」に置く。** 市場確率に近づくこと（`R-029`）はその手段であり到達点ではない——**情報捕捉率 0.75 のモデルは、オッズ板を見れば無料で・即座に・より正確に得られるものの劣化再現でしかない。**

`D-002`（対象レースのオッズを特徴量にしない）は維持するが、**根拠を取り直した**。元の根拠「市場を超える余地を残す」は `D-119` が失効させたため、新しい根拠は「**将来新しい情報を投入したときに、その情報が市場に対して上乗せを持つかを測れる状態を保つため**」とする。オッズを入れるとモデルと市場が交絡し、`R-030` の測定が原理的に成立しなくなる。

**`R-029` は単勝オッズを特徴量に入れれば即座に `1.0` に到達する。** 禁じているのは `D-002` だけであり、`R-029` は「`D-002` の制約下での」達成度を測る指標である。報告時にこの前提を省略しない。

### 次に手を付けるもの

**新データの当てが無くなった（`D-125`）。**

JRDB の過去データ契約は費用の判断で**当面見送り**（保留であって否定ではない。なお過去データの価格は公開されておらず、調査でも確認できなかった）。無料代替として netkeiba の調教データを実地確認したが、**ハロンタイムはプレミアム限定で取得できない**——過去G1の追い切りページはタイム非表示（評価A〜Dと短評のみ）、馬ごとの調教履歴ページはタイムが伏せ字。netkeiba の無料範囲は開催に紐づくローリング窓であって、2015-2024のバックテストに使えるアーカイブではない。`D-005` の「無料では取得困難」という見立ては、これで実地確認済みになった。

**`D-124` が定めた目的（新データへの土台）は維持するが、その先に載せるものが現時点で存在しない。** 土台整備は進められるが、それを承知のうえで進めることになる。

**`F-304` は実装・測定とも完了した（`D-126`）。**

| 母集団 | `v9`（F-304なし） | `v10`（F-304あり） | 対比較 |
|---|---|---|---|
| G1（135R） | 0.5468 | **0.5308** | +0.0126 [−0.0596, +0.0873] **差なし** |
| all（23,224R） | 0.6271 | **0.6767** | −0.0331 [−0.0367, −0.0294] **有意に改善** |

**`F-304` は維持する。** G1の後退はノイズ（135レースでは ±0.016 は判定できない）であり、`all` の有意な改善を捨てる理由が無い。

**これを受けて `R-029` の判定母集団をG1から `all` に変更した（`D-127`）。** G1ではこの規模の差を検出できない——2番目に強い特徴量を投入しても `+0.0126 [−0.0596, +0.0873]` とノイズに埋もれ、fold あたり12〜27レースの母集団は改善を積み上げる作業の進捗計を務められない。情報捕捉率は尺度を持たない指標として設計してあるため（`D-122`）、母集団を変えても閾値 `0.75` の意味は変わらない。

**これは実質的に基準を緩める変更である。** 合格までG1では `0.22` あったものが `all` では `0.073` になり、**`R-029` は `D-001` が定めたG1スコープに対する達成度を測らなくなった。** G1に向き合う要件は `R-023`（達成不可能と確定済み）と `R-027`（年代別の併記）が担う。

### 次に手を付けるもの

**`all` の情報捕捉率を `0.6767` から `0.75` に近づける**（`R-029`）。必要なLogLoss改善は **`2.1522 → 2.1033` の `0.0489`**。`D-119` が禁じたのは市場を**超える**ことであって、市場に**近づく**ことは否定されていない。

**刈り込みは経路にならない（`D-128`）。** 現構成でグループ別アブレーションを測り直したところ、**外した方が良いグループは1つも無かった**（14グループ × 3 fold の42セルすべてが正）。`D-103` の診断3（外した方が良いものが並ぶ）は `D-109` バグ修正前・`D-113` 正則化前・`F-304` 実装前・fold 0単独・CIなしの測定であり、現構成の判断材料として引用してはならない。

寄与の順位は3 foldで安定している——`F-601 前走`（+0.036〜+0.038）> **`F-304 速度指数`（+0.025〜+0.028）** > `F-7xx 人的ID` > `F-303 上がり3F` > `F-603 馬体重`。

**参考: これまでの単発の改善幅**は `D-113`（正則化）が `0.0341`、`F-304` が `0.0331`。**同規模が2件あれば `0.0489` に届く計算**になる。

**2つの経路は閉じた。** 刈り込みは `D-128`（外した方が良いグループは1つも無い）、ハイパーパラメータ探索は `D-129`（上限が `0.005` 前後で必要量の約10%）。条件付き速度指数（適性）も不発だった（`D-132`）。

**効いた2つは既定に採用した（`D-133`）。**

| 施策 | 内容 |
|---|---|
| **Plackett-Luce top-3**（`D-130`） | `lambdarank`（NDCG最適化）を `R-029` が測る勝利確率の尤度そのものに替えた。`lr=0.02`・`min_sum_hessian_in_leaf=1.0` と組で使う |
| **`F-805`**（`D-131`） | `age`/`sex`/`weight_carried`。取り込み済み・欠損ゼロなのに使われていなかった実装漏れ |

7 fold walk-forward（147.8分）で `all` 捕捉率 **`0.6767 → 0.6951`**（G1 は `0.5308 → 0.5932`）。**学習期間OOFで予測した積み上げ `0.0135` に対し実測 `0.0122` と一致した**——`D-098` のクロスフィッティングが7fold検証の代理として機能することの確認にもなり、以降の探索を1件30〜60分で回せる根拠になる。

### `R-029` の現在地と方針（`D-134`）

**`all` 捕捉率 `0.6951`、合格 `0.75`、残り `0.0549`**（LogLoss換算 `0.0367`）。**閾値 `0.75` は維持する**（`D-134`）——`v9` の `0.6271` から1セッションで `+0.068` 動いており射程内と判断した。下げると指標が事後的な定義になる（`D-030` の失敗の裏返し）。

**不利な材料も記録しておく**: 簡単な当たりは使い切った（未使用列・目的関数・速度指数）。外れた候補はいずれも「既にある情報の焼き直し」だった。上限は `1.0`（市場と同等）で `D-119` により超えられない。

### 探索の指針が2つ立った（`D-132` / `D-135`）

**木が自力で作れる情報を渡しても効かない（`D-132`）。** 条件付き速度指数（`surface` 別・距離帯別の適性）が不発だったのは、モデルが `f803_surface` と `fspd_*` の交互作用を自力で構成できたため。

**その対として、モデルが原理的に構成できない情報は効く（`D-135`）。** 該当するのは**他の行にまたがる集約**——モデルは対象馬の過去成績は見ているが、**その過去走で誰と走ったか**は別の行にあり到達できない。相手強度（`F-806`）は3 fold とも有意に改善した（−0.0061 / −0.0066 / −0.0045）。

**ただし指針には限界がある（`D-136`）。** 「モデルが構成できない情報」は**必要条件であって十分条件ではない**——`course`・斤量の自己履歴・馬体重の自己履歴はいずれも不発だった。**既存列と重複していれば効かない。** `F-806` が効いたのは、対戦相手の質が既存のどの列にも入っていなかったため。次に候補を立てるときは「構成できないか」に加えて「**既存列と重複していないか**」を確かめる。

なお `course`（競馬場）が特徴量に無いのは実装漏れではなかった。追加すると2 foldで有意に悪化する——`f803_distance`/`surface`/`direction` が実質的にコースを絞り込み、`F-802` が馬自身のコース別成績を持つため。`D-131`（`age`/`sex`/`weight_carried`）と表面上は同じ「使われていない列」だが、結論は逆だった。

### 2条件を満たす候補は当たった（`D-137` / `D-138`）

**`F-807`（前走からの条件替わり）** が `D-136` の2条件を両方満たす初めての候補で、12セルすべてが有意に改善した。`f601`/`f602` は前走の着順・着差・週数・グレードを持つが、**どんな条件のレースだったかを持たない**——距離延長/短縮や芝ダ替わりが表現できていなかった。

**`F-806` と `F-807` は超加算になる（`D-138`）。** 同時投入すると3 fold とも単体の和を上回り、合算は **`−0.0115`**（単純和 `−0.0097`）。両者は同じ過去走に由来するが軸が直交している——`F-806` は「**誰と**走ったか」、`F-807` は「**どんな条件で**走ったか」。組み合わせて初めて「強い相手に、今回と同じ条件で走った実績」が識別できる。

必要な `0.0367` の **31%**にあたり、7fold確認に進む水準に達した。

### 7fold で確認した（`D-139`）

`all` 捕捉率 **`0.6951 → 0.7131`**（+0.0180、156.7分）。学習期間OOFの予測 `−0.0115` と7fold実測 `−0.0120` がほぼ一致し、`D-133` に続き2度目の的中——クロスフィッティングが7fold検証の代理として機能することの信頼性がさらに固まった。`F-806`/`F-807` は既に `FEATURE_FNS` に結線済みのため、追加のコード変更なしに既定へ反映されている。

**測定中に寄り道があった。** 1回目の実行で fold 0 が51分を超えたため異常と判断して停止し、一時ファイルの肥大化から `F-806` のSQLを原因と誤診した。実際に全特徴量関数を実測すると `F-806` は1.5秒で無関係、**行列組み立ての97%は既存の `compute_f303`（239.5s）と `compute_f703`（152.0s）だった。** 誤診に基づく「最適化」は値こそ一致したが実行時間を1.1秒→451秒に悪化させており、変更前に新旧を比較する検算をしていたおかげでその場で気づいて撤回できた。1回目の遅延そのものの原因は未特定（再実行では正常に完了）。**教訓: 実行時間の異常は、まず実測してから原因を特定する。**

**`R-029` の現在地: `all` 捕捉率 `0.7131`、合格まで `0.0369`。** `v9` の `0.6271` から1セッションで `+0.086` 動いた。

### 3つ目の候補（`F-808`）は7fold実測で覆った（`D-140`〜`D-142`）

`F-701`/`F-702`/`F-703` のいずれも持たない「騎手×馬のコンビでの過去成績」を `F-808` として追加した。学習期間OOFでは `F-806`+`F-807`+`F-808` の合算が3 fold とも有意に改善（平均 `−0.0093`）していたが、**7fold walk-forwardでは `all` が有意に悪化した**（LogLoss `+0.0016` [+0.0003, +0.0030]、年代別7年中5年が悪化）。G1は逆に複数年で改善したが、判定は `all`（`D-127`）。

**`F-808` を既定から除外し、`v12`（`F-806`+`F-807`のみ）に戻した。** `R-029` は `0.7131` のまま。

**これは重要な反例になった。** `D-133`/`D-139` で2度確認していた「学習期間OOFの予測が7fold実測と一致する」という前提が、今回初めて破れた（`D-142`）。学習期間OOFは有力な予備選抜だが、7fold確認を省略してよい根拠にはならないことが実測で裏付けられた——もし学習期間OOFだけで採否を決めていれば、有害な変更を本番に入れていたことになる。

**`F-808` 単体での切り分けが完了した（`D-143`、`Q-045` 解決）。** `v11`+`F-808` を7fold確認したところ `all` は差なし——`D-142` の悪化は `F-808` 固有ではなく `F-806`/`F-807` との組み合わせに起因すると判明した。`F-808` は無害だが採用理由もないため既定に入れない。相互作用の機構は未解明のまま優先度を上げない（`F-808` 単体が無効な以上、解明しても `R-029` への効果が見込めない）。

### 候補選定の法則を抽出した（`D-145`）

本セッションの候補10件を分類したところ、**当たり4件がすべて「新カテゴリ」、外れ6件がすべて「既存カテゴリの変奏」**と例外なく分かれた。変奏は定義上「既存列の切り口違い」なので `D-132`（木が自力で作れる）か `D-136`（重複）に引っかかりやすい。**この法則があれば `F-808`（`D-143`）とクラス変化（`D-144`）は試す前に落とせていた。**

法則に従って探し直した結果、**馬自身の通算成績を持つ列が一本も無い**ことが判明した（`f702_jockey_experience_count` は騎手、`F-802` は条件別、`F-601` は前走のみ）。`F-809`（キャリア成績率）として実装し、7fold確認で採用した（`D-146`）。

### `R-029` の閾値を撤回した（`D-147`）

**到達点 `0.7169`**（`all` 母集団の情報捕捉率、`v9` の `0.6271` から `+0.0898`）。合格閾値 `0.75` は撤回し、`R-029` を**「到達点を記録する要件」**に性質変更した。

| 版 | 捕捉率 | 差 | 内容 |
|---|---|---|---|
| `v9` | 0.6271 | — | 出発点 |
| `v10` | 0.6767 | **+0.0496** | `F-304` 速度指数 |
| `v11` | 0.6951 | +0.0184 | PL目的関数 + `F-805` |
| `v12` | 0.7131 | +0.0180 | `F-806` + `F-807` |
| `v15` | **0.7169** | **+0.0038** | `F-809` キャリア成績率 |

**当たりの効果量が一貫して逓減している。** 残り `0.0331` を埋めるには同水準の当たりが8件以上必要で、`D-134` が閾値維持の根拠とした「勢い」は失われた。

**「達成」とは呼ばない。** 当初検討した `0.72` への引き下げは現在地との差が `0.0031`（測定ノイズと同オーダー）で、`D-030`/`D-134` が警告した「届きそうな水準まで下げる」形にあたるため。閾値を動かして達成を作るのではなく、閾値そのものを撤回して到達点を事実として残す。

**この決定が失うもの**: `R-023`（旧A判定）は `D-119`/`D-120` で達成不可能と確定済みであり、`R-029` も判定要件でなくなったため、**このプロジェクトは「モデルとしての成功」を判定する要件を持たない状態になる。**

前走からのクラス変化（昇級・降級）も試したが不採用だった（`D-144`）。`D-136` の2条件は満たしていたが、3 fold中2 foldが差なし/悪化で一貫しなかった。単変量の健全性は直感と逆で（昇級馬の勝率が最良）、面白い発見ではあったが有用性には結びつかなかった。

### `F-103` の縮約問題は決着した（`D-116` 〜 `D-118`）

`Q-044` を解決した。`F-103` の `β` は `F-302` と**同型の未ロバスト化問題**を抱えていることを実証した（`D-116`）——`β` の std は過去走2走の馬で131.5・21走以上で3.03（43倍差）、`k=5` の縮約は `n=2` で `β` の29%を残すため無力、fold 0 行列の `f103` 最小値 `-1192.11` は過去2走の `p_i` がほぼ同値だった1頭に特定できた。

しかし対処候補5つ（`MIN_VAR_X` 引き上げ・`n_valid_pairs` 下限・`mu_global` 中央値化・併用・単純除去）をG1 OOFで3 fold 比較したところ、**向きが3 fold 揃う候補は1つも無かった**（`D-118`）。**丸ごと除去しても base と区別がつかない**ことから、`F-103` は有害なのではなく**不活性**と判明した——`D-113` の `cat_smooth`/`cat_l2` を含む現在の設定下で LightGBM が実質的に無視できている。

`Q-042`（`F-301`/`F-302`）とは同一の設計課題（精度を無視した標本数だけの縮約）だが、**`F-103` 単体では共通基盤への作り直しを正当化できない**ことが分かった。`F-301`/`F-302` 側で効果が実証されてから展開する順序にする。

`F-302`が`all`母集団を悪化させる要因は`D-114`で分解診断まで完了している。生値の無制限な尺度が主犯の一つと確認したが、relativize後も残る有害性は`F-301`の推定方法自体の再設計が要ることが分かったため、次善の手としては着手しない（保留）。

### 未決のうち優先度が高いもの

`Q-017`（Plackett-Luce の Harville bias）・`Q-018`（複勝・ワイドの過去オッズ）・`Q-019`（発走15分前の締切）。`Q-042`（`F-302`が`all`を悪化させる要因）は分解診断まで完了し優先度を下げた——`F-301`の推定方法自体の再設計が要る大きな課題であり、次善の手として着手するものではない。`Q-017` と `Q-018` は `P-4` の前提であり、`P-4` 自体が上記の理由で止まっている。

### 特徴量探索を2件試し、どちらも外れた（`D-148`〜`D-150`）

`R-023`/`R-030` が達成不可能と確定し `R-029` も閾値を撤回した後（`D-147`）、`D-145` の法則（新カテゴリは当たり、既存カテゴリの変奏は外れ）に従って新カテゴリを2件探索した。

**`F-502`（当日時計傾向）**: `Q-031`（基準タイム未定義）を `D-148` で解決し（`F-304` の `base(...)` を再利用）実装・テストしたが、学習期間OOFで3 fold中1 foldが有意に悪化・2 foldが差なしで**不採用**（`D-149`）。コードは `src/umagic/features/f502.py` に残すが `FEATURE_FNS` には結線していない。

**騎手の当該開催での調子**: `F-701`（通算）にも `F-703`（遠征/主戦フラグ）にも無い「直近の勢い」を狙った候補。`D-136` の2条件（モデルが構成できない／既存列と重複しない）は満たしていたが、3 foldの符号が「悪化→差なし→改善」と一貫せず**不採用**（`D-150`）。プロトタイプ段階（`data/diag42_jockey_meeting_form.py`）で止め、`src/` には実装していない。

**`R-029` の到達点は `0.7169`（`D-146`）のまま変わらない。** `D-136` の2条件を満たす候補が2件連続で実測に外れたことは、`D-147` が示した逓減トレンド（当たりの効果量が `0.0496 → 0.0184 → 0.0180 → 0.0038`）を補強する——設計上「筋が良い」候補でも当たる保証がない段階に来ている。

### `JRDB` への問い合わせは行わない方針（`D-151`）

`R-023`/`R-030`（`P-4`）を開ける既知の唯一の経路は新データ（`D-124`）であり、その最有力候補が `JRDB` だった。`Q-002` の調査で価格自体は安いことが分かったが、過去データ（2015-2024）の遡及可否・価格だけは公開情報で埋まらず、埋めるには直接の問い合わせが必要だった。**この問い合わせをユーザー判断で行わない方針とした（`D-151`）。** 理由は特に確認していない。

**これにより、`P-4` を開ける既知の経路が無くなった。** `netkeiba` は無料範囲で取得不可と確認済み（`D-125`）であり、他に具体的な新データ源の候補は無い。

### 着眼を転換してプール構造を探索した（`D-152` 〜 `D-155`）

`D-151` の後、**「市場を超える」から「プール間の食い違いを突く」に着眼を転換**して探索した。`D-119` が実証したのは「単勝プールを超えられない」ことだが、これは**「単勝プールの価格は正しい」とも読める**。競馬にはプールが複数あり、それぞれ別の投票・別の計算難度を持つ。プール間の食い違いは、馬について市場より詳しくなくても利益になりうる——**`D-119` の否定ではなく `D-119` を土台として使う**着眼である。モデルを一切使わず単勝オッズと実払戻だけで完結するため、`Q-018`（複勝の過去オッズが取れない）にもブロックされない。

**見つかったもの**（`D-152`、33,200レース・465,626出走行）:

| プール | 構造 |
|---|---|
| 単勝 | `0.77`〜`0.80` で**平坦**。控除率どおりで極めて効率的（`D-119` と整合） |
| **複勝** | `0.9637` → `0.5171` の**強い単調勾配**。単勝には無い歪みが複勝にだけある |
| ワイド・馬連・馬単・三連複・三連単 | `0.68`〜`0.80`。**全滅**。利用できる歪みが無い |

**`Q-017`（Harville bias）を実データで解決した（`D-153`）。** 起票時の予想どおり、**人気馬の複勝確率を最大 10.6 ポイント過大評価**する（予測 `0.825` → 実測 `0.719`）。交差点は `0.32` 付近。`P-4` に進む場合、素の Harville では人気馬を系統的に買いすぎることになる。実測表がそのまま較正表になる。

**相手関係が効くことも分かった（`D-154`）。** 自身の単勝オッズ帯を固定しても、Harville複勝確率の高低でROIが有意に変わる（`1.5〜2倍` 帯で `0.9077` 対 `0.8439`、CI非重複）。**複勝プールは相手関係を十分に織り込んでいない。**

**しかし `1.0` を超えない（`D-154`）。** 閾値を連続的に掃いた上限は `0.983`〜`0.991`。**単勝1.5倍未満の複勝は10年すべてが `1.0` 未満**（`0.9184`〜`0.9968`）で、上位2000頭でも 0/10年。**控除率20%のうち18ポイントを取り返せるが、最後の1〜2ポイントが埋まらない。** 構造は極めて安定して実在するが、安定して負けている。

**モデルは複勝でも上乗せを持たない（`D-155`）。** `D-119` は勝率LogLossを測っており「人気馬に限った複勝の当落」は測っていなかったが、これも実測して否定された——市場PL上位1000頭 `0.9622` に対し、モデルで絞り込んだ同数が `0.9612`。**`D-119` は目的変数と部分母集団を替えても成立する。**

### 壁の理由が判明した——元返しによる構造的天井（`D-156`）

`D-154` の上限（`0.983`〜`0.991`）は探索不足による経験的な壁ではなく、**JRA の元返し（最低払戻100円）から導かれる構造的な天井**だった。

`ROI = 複勝率 × 払戻倍率` と分解すると、両者が逆方向に動いて積が `1.0` 未満で閉じる:

| PL複勝確率 | 複勝率 | 払戻倍率 | **元返し率** | ROI |
|---|---|---|---|---|
| 0.624 | 0.560 | 1.497 | 0.1% | 0.8387 |
| 0.922 | 0.837 | 1.096 | 4.6% | 0.9180 |
| **0.967** | **0.930** | **1.057** | **43.1%** | **0.9828** |

**最上位帯では的中の43.1%が元返し**。複勝率を上げようと極端な人気馬に向かうほど払戻倍率が `1.0` の床に張り付く。**「もっと極端な人気馬を探す」方向は原理的に無意味**だと確定した。

一般形では利益に `P_true / P_market > 1/(1−t) = 1.25` が必要で、実測は `1.2240`——**favorite-longshot bias が控除率20%のうち18ポイントを取り返すが、最後の2ポイントを元返しが塞ぐ。**

**未着手だった24スライスも全滅**（馬場状態4・芝ダ2・競馬場10・レース番号4・クラス4）。値が `0.87`〜`0.92` に密集し、点推定が `1.0` を超えたセルは1つも無い——この歪みは条件によらない**市場全体に一様な現象**である。

### `Q-018` の余地を定量化した（`D-157`）。候補としては残す

元返しは**実際の払戻**にかかる制約なので、複勝オッズ板が見えれば元返しの馬を避けられ、天井が動く可能性があった。**定量化した結果、余地は薄い。**

元返しを避ければ倍率は `1.06 → 1.10` に上がり、複勝率が変わらなければ **+1.5ポイントの黒字**。しかし余裕は `+0.015` しかなく、**黒字が見える領域では複勝オッズがほぼ動かない**（PL複勝確率 `0.923` 帯の四分位が `[1.10, 1.10]`、変動係数 `0.022`）。払戻が10円刻みで下限100円のため取りうる値が実質2つしかなく、**板を見ても選ぶ対象が存在しない。** さらに元返しになるのは群衆が最も確信している馬であり、避ければ複勝率が下がる（逆選択）。

**`Q-018` は候補として残す（ユーザー方針）。** 優先度は「高」から「中」に下げたが可能性は閉じていない。取得できた場合の判定基準（高払戻側で複勝率が `1.5` ポイント以上落ちないか）と取得経路の候補（`JRA-VAN` 未調査 / Webアーカイブ / 今から自前収集）は `D-157` と `Q-018` に記録した。

**なお `D-156` の「元返しが塞ぐ」という記述は `D-158` で訂正した。** 複勝払戻に100円未満は1件も無く、元返しは払戻を**引き上げる補助**である。塞いでいるのは控除率そのもので、元返しの役割は極端側の上限を複勝率で頭打ちにすること。`D-156` の結論（上限が構造的、探索方向が無意味、24スライス全滅）は変わらない。

### `JRA-VAN` に新データがあった。`D-125`/`D-151` の前提が崩れた（`D-159`）

`Q-002` が調べたのは `JRDB` のみで、**JRA公式系の `JRA-VAN DataLab` は未調査だった。** 公開情報のみで調べた結果（`D-151` が禁じた問い合わせは行っていない）:

| 項目 | 内容 |
|---|---|
| 料金 | **月額 2,090円（税込）**。`JRDB`（2,380円〜）より安い |
| 申込 | **オンライン完結。問い合わせ不要** |
| **坂路調教データ** | **2003年以降。** バックテスト期間（2015-2024）を完全にカバー。年間約60万件 |
| 動作環境 | **Windows 10/11 専用**（`JV-Link` 経由） |

**`D-125`（新データの当てが無い）と `D-151`（`P-4` を開ける既知の経路が無い）の前提は事実として崩れた。** `JRDB` で最大の未確認事項だった**遡及可能年が、`JRA-VAN` では公開情報として判明している**（2003年以降）——問い合わせなしで判断材料が揃う。

**ただし留保が3つある**: (1) 公式FAQが「かなり不完全」と明記し**追い方・合わせ馬が含まれない**ため、実装できるのは `F-401` のみで `F-402`/`F-403` は不可、(2) 誰でも買える公開情報のため **`D-119` の壁が崩れる保証は無い**（`D-120` と同じ構造になりうる）、(3) **Windows 専用**で開発環境（macOS）から直接使えない。

### `JRA-VAN` は脱落した。macOS で使える経路は2つ（`D-160`）

`JV-Link` は **ActiveX COM コントロールで Windows 専用**。macOS版SDKも REST API も存在せず、**ユーザーが Windows を導入しない方針のため `JRA-VAN` は契約の是非以前に使えない**（`D-160`）。

**これで `D-159` の比較が逆転した。** プラットフォーム適合性という決定的な軸を見落としていた:

| | `JRA-VAN` | `JRDB` | `netkeiba` スーパープレミアム |
|---|---|---|---|
| macOS | **不可** | **可**（LZH/ZIP のテキスト） | **可**（Web） |
| 料金 | 2,090円/月 | 2,380円/月〜 | **1,390円/月** |
| 調教データ | 追い方・合わせ馬が無い | **最も濃い**（`CHA`+`CYB` で `F-401`/`F-402`/`F-403` をすべて実装できる） | 重賞・新馬中心 |
| 障害 | **Windows専用** | 過去データの可否は非公開（問い合わせが要る＝`D-151`） | 遡及範囲不明・**有料コンテンツの規約確認が要る** |

**`JRDB` の調教データが3者で最も濃い**ことが仕様書（無料公開）の確認で分かった——`CHA` にハロンタイム・区間タイム・追い方（一杯/強め）・馬体評価、`CYB` に併せ馬・調教タイプ・体格評価。**問い合わせは行っていない**（`D-151` を守っている）。

**`D-151` が決められた時点と情報状態が変わっている。** `D-151` は `JRA-VAN` の存在も、それが Windows 専用であることも、`JRDB` の調教データが最も濃いことも分からない状態で決められた。**`D-151` は撤回しないが、現時点では `JRDB` が「macOS で使える唯一の完全な調教データ源」である。**

**`netkeiba` 有料には規約の論点がある。** `D-014` は**無料公開ページ**の取得を条件付きで許容したが、**認証の背後にある有料コンテンツへの自動アクセスは検討していない。** 確認せずに着手してはならない。

判断は `Q-046` に登録した。**(a) `D-151` を維持して `JRDB` を諦める / (b) `netkeiba` 有料の規約を確認する / (c) 新データを諦めて現状を最終形とする** の3つに分かれ、いずれも新たな `D-xxx` を要する。新データを入れる場合、`F-401` の作り込みに投資する前に **`D-119` の上限測定を調教データ込みで再実行する**のが最小コストの判定になる。

**現状の総括**: `R-029` の到達点 `0.7169`、`R-023`/`R-030` は現データでは達成不可能、**利益の出る馬券戦略は現在のデータでは見つからず、その理由も構造的に説明がついた**（最良で `-1〜2%`／賭け、控除率のままなら `-20%`）。**ただし `D-159` により、未取得の新データ源が1つ実在することが判明した。**
