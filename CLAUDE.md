# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# プロジェクト概要

JRA平地G1（年24競走前後）に限定した競馬予想モデル。出走全頭の勝率を出力し、期待値ベースで馬券を選ぶところまでを範囲とする。

**`P-0`〜`P-3` は完了済み。`P-4` は着手できない状態にある。** `src/umagic/` に実装があり、テストは383件。データは2015-2024の10年分（33,236レース・467,927出走行）を `data/umagic.duckdb` に取り込み済み。

`P-3` は実装完了だが `R-023`（G1でLogLossが市場確率を下回る）は未達（モデル 2.3085・市場 1.8912、`D-113`確定後の基準値）。要因診断の結果、**現在のモデルは市場確率に対する上乗せ情報を持たない**（対数線形ブレンドの最良重みが `w_model = 0.00`、23,224レース）ため、期待値計算に進めば控除率20%ぶんそのまま負ける。**`P-4` に進む前に予測性能を詰めること**（`D-103`）。現状と次の一手は `docs/README.md` の「現在の状態」にある。

**`F-301`/`F-302`（馬場差推定）は実装済みだが既定で無効**（`Stage2FoldRunner.include_track_variant=False`、`D-110`）。汚染されていない基準で測定したところ `all` 母集団を7fold全てで悪化させた。`Q-042`（メンバーレベル混入）を検証し`grade`固定効果を追加したが（`D-111`）、G1側の悪化は半減した一方`all`側はほぼ無風（`D-112`）——`all`の悪化要因は依然不明。有効化する前に原因を調べること。

> このファイルは変更ログとして使用しない。決定の履歴は `docs/decisions.md` に記録すること。

## ディレクトリ構成

| パス | 役割 |
|---|---|
| `docs/requirements.md` | 満たすべき要件（`R-xxx`） |
| `docs/decisions.md` | 設計判断と根拠（`D-xxx`） |
| `docs/domain-knowledge.md` | 競馬ドメイン知識・特徴量カタログ（`F-xxx`） |
| `docs/architecture.md` | 構成・スキーマ・評価設計・Phase計画（`P-x`） |
| `docs/open-questions.md` | 未決事項（`Q-xxx`） |
| `docs/spec/` | 実装仕様書（`001`〜`007` / `012` / `014` / `015` の10件。いずれも `Draft`） |
| `docs/tasks.md` | 実作業と完了条件（`P-0`〜`P-3`） |
| `src/umagic/` | 実装本体。構成は `docs/architecture.md` 5節 |
| `tests/` | テスト383件。`-m realdata` は実データを要しCIから除外（`D-053`） |
| `scripts/` | 取り込み・血統取得などの実行スクリプト |
| `.claude/skills/spec-hearing/` | 要件・仕様のヒアリング用スキル |
| `.claude/skills/task-breakdown/` | タスク分解用スキル |
| `tools/q011_feasibility/` | `Q-011` 確認用の使い捨てスパイク。`src/umagic/` の一部ではない |

**`data/` は `.gitignore` 対象**（`R-017` / `D-014` 条件1: 再配布しない）。生HTML・キャッシュ・DuckDB・実験用スクリプトはここに置き、コミットしない。

`src/umagic/` 以下の構成は `docs/architecture.md` 5節にある。ここには再掲しない。

**`design.md` は作らない（`D-019`）。** 設計は `architecture.md`（全体）と `docs/spec/`（モジュール単位）が担う。

## 技術スタック

**`D-042` で確定済み**（`Q-008` は Resolved）。

| 用途 | 選択 |
|---|---|
| 言語 | Python **3.12+** |
| データ処理 | **Polars** に一本化（pandas を使わない） |
| ストレージ | DuckDB（`D-036`） |
| モデル（Stage 2） | LightGBM `lambdarank` |
| レポート | Markdown ＋ matplotlib（`D-004`） |
| 依存管理 | **uv**。`uv.lock` をコミットする |

混合効果モデル（`F-301`）の実装方法は確定済み（`D-104`〜`D-107`、自前の交互反復、`Q-025` Resolved）。**階層ベイズは `D-051` で対象外になった**（`F-103` / `F-202` は `F-902` の単純縮約式に統一）。ただし `F-302` 自体が実測で有害と判明し既定無効（`D-110`）。次は原因究明（`Q-042`）。

## よく使うコマンド

```bash
uv sync                                   # 依存の同期
uv run pytest tests/ -q -m "not realdata" # CIと同じ範囲。実データ不要
uv run pytest -m realdata                 # 実データ（data/umagic.duckdb）を要する（D-053）
uv run python scripts/ingest_range.py --help   # 取り込みと品質検査
```

**`data/umagic.duckdb` を要する処理は重い。** `P-3` の7 fold walk-forward（`run_p3_completion_check()`）は約101分かかり、その大半は `build_features()` の履歴自己結合が占める。fold 1本ぶんの特徴量行列の組み立てだけで9〜11分・ピーク2GB前後を使うため、**探索的な比較をするときは行列を parquet にキャッシュして使い回すこと**。

`Q-001`（利用規約）は `D-014`、`Q-011`（データ取得可否）は `D-023` で解決済み。ただし `D-014` は個人利用・再配布しない・レート制限とキャッシュの実装を条件としており、`D-023` は複勝・ワイドの過去オッズが取得できないことを `Q-018` として残している。

## 設計原則

以下は直感に反するため、理由を知らないと自信を持って間違える。

- **学習はG1に絞らない（`D-003`）**: G1は20年遡っても約7,700出走行しかなく学習データとして成立しないため、JRA全レースで学習し、クラス特徴量・`sample_weight`・G1での確率校正で特化させる。「G1に絞る」をデータフィルタとして実装しないこと。現在取り込み済みなのは10年分（2015-2024）の467,927出走行。

- **予測対象レースのオッズを特徴量にしない（`D-002`）**: 入れると精度は上がるが、市場を超える余地が消えるため。目標は `normalize(1/単勝オッズ)` で定義される市場確率のLogLossを下回ることであり、オッズは最終段の期待値計算でのみ使う。例外は `F-701`/`F-703` が騎手・厩舎の交絡除去に使う**過去レースの**人気のみ（`Q-006`）。この線引きを勝手に広げない。

- **モデルが2段階なのはリーク回避のため（`D-007`）**: 対象レースの実測ラップは発走後にしか判明しないため、Stage 1（レース質予測）で発走前に推定する。実測ラップはStage 1の目的変数としてのみ使い、特徴量に混入させれば即リークになる。

- **同日レースの扱いに例外規定がある**: 過去成績の集計は `race_date < target_race_date` で厳密にフィルタするのが原則。ただし `F-501`/`F-502`（当日馬場バイアス）だけは例外で、**対象レースより前に発走した**同日のレースを使ってよい。G1がメインレース（10R〜11R）であることを利用している。この例外を他の特徴量に広げないこと。

- **回収率を額面通りに報告しない（`D-008`）**: 検証に使えるG1は約240レースで、単勝の回収率の標準誤差は `4/√240 ≈ 26%`。**回収率74%〜126%の範囲では有意差を主張できない**ため、報告時は必ずブートストラップ信頼区間を添える。主評価券種は分散の小さい複勝・ワイド。3連単は控除率27.5%かつ超高分散で検出力が低く対象外。

- **直近のG1は封印されている（`D-017`）**: 封印セットは「まだ開発に使っていない直近3年のG1」で、固定の年ではない。開発中は一切参照しない。**学習データ（全レース）は封印対象ではない**ので、開発のフィードバックは全レースで回す。封印セットを開いたら回数を記録し、報告に必ず添えること。禁止ではなく計上する規律。ドリフトは年代別成績で見る（`D-018`）。

- **G1では一般的な競馬予想の知見が反転する**: 定量・馬齢重量が中心なので斤量の情報量が低く（ハンデ戦と真逆）、前走着順も前哨戦が叩き台になるため結びつきが弱い。ハンデ戦・条件戦を前提にした特徴量設計をそのまま持ち込まないこと。詳細は `docs/domain-knowledge.md` 3節。

- **ハイパーパラメータの選定指標はG1に絞る（`D-101` / `D-103`）**: 全レースの LogLoss で候補を選ぶと、G1性能を悪化させる候補を最良と誤判定する。**これは2度起きている。** 1度目は `class_weights` で、学習期間末尾1年の inner LogLoss が最良だった候補を採用したところG1が `2.2985 → 2.4030` に悪化した。2度目は生IDの扱いで、`all`（9,947レース）では3 fold とも有意に改善する「ID除去」がG1 OOF では3 fold とも悪化した。`R-023` が問うのはG1でのLogLossなので、選定にも `D-098` と同じクロスフィッティングで作った**G1のOOFスコア**を使うこと。全レースの指標と目的指標は方向が一致するとは限らない。

- **G1の標本は常に足りない**: 検証期間のG1は fold あたり24〜27レース、7 fold 全体でも135レース。この規模ではLogLossの差 0.1 程度は判定できない（`D-008` が回収率について言っていることと同じ問題）。差を主張するときは対比較のブートストラップ信頼区間（リサンプリング単位はレース、`D-078`）を必ず添え、**単一fold・単一指標の結果を採用しない**。標本を増やすには学習期間のG1をクロスフィッティングでOOF評価する（fold 2 で119レース）。

リーク防止の全7原則は `docs/domain-knowledge.md` 5節にある。原則7（集計統計量の推定期間も as-of で切る）は日付フィルタの検査では捕まらないので特に注意。`tests/test_leakage.py` として実装済みで、CIで常時検証している（実データ版は `tests/test_leakage_realdata.py`、`pytest -m realdata`）。

## ドキュメント規約

ドキュメントは日本語で書く。

**「なぜ」と「何を作るか」を意図的に分離している。** 根拠は `docs/decisions.md` に `D-xxx` として置き、仕様書（`docs/spec/`）からは `D-xxx` を参照するに留める。仕様書に根拠を書き込むと、仕様変更のたびに根拠が失われ、根拠が変わったときに影響範囲を追えなくなる。

`docs/` 配下を編集するときは `.claude/rules/docs-writing.md` を読むこと。ID体系の運用と仕様書の書き方がある。

## 参考ドキュメント

- 全体像とドキュメントの地図: `docs/README.md`
- 特徴量を実装するとき: `docs/domain-knowledge.md` の `F-xxx`
- 着手前に確認すべき未決事項: `docs/open-questions.md`
