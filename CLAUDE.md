# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このリポジトリの現状

**構想フェーズ。実装コードは1行も存在しない。** 中身は `README.md` と `docs/` のみ。

ビルド・テスト・実行コマンドは**まだ存在しない**。`docs/open-questions.md` の `Q-008` に想定スタック（Python 3.11+ / Polars / DuckDB / LightGBM）が書いてあるが、これは**暫定案であって確定事項ではない**。確認なしにプロジェクトの雛形を作らないこと。

着手のブロッカーは `Q-001`（データ取得の利用規約確認）。これが未解決のままスクレイパーを書き始めてはならない。

## ドキュメントの規約

ドキュメントは日本語で書く。

### 役割分担を守る

「なぜ」と「何を作るか」を意図的に分離している。**この境界を壊さないこと。**

| ファイル | 書くこと | 書かないこと |
|---|---|---|
| `docs/decisions.md` | 設計判断とその根拠 | 実装の詳細 |
| `docs/domain-knowledge.md` | 競馬ドメインの知識、特徴量カタログ | 実装の詳細 |
| `docs/architecture.md` | 構成・スキーマ・評価設計・Phase計画 | 個別の実装仕様 |
| `docs/spec/` | 実装仕様 | **判断の根拠**（`D-xxx` を参照するに留める） |

仕様書を書いている最中に新しい設計判断が発生したら、**まず `decisions.md` に `D-xxx` を追加してから**仕様書側で参照する。仕様書の本文に根拠を書き込まない。

### ID体系

`D-xxx`（決定）/ `F-xxx`（特徴量）/ `Q-xxx`（未決事項）/ `P-x`（フェーズ）。
**一度振ったIDは再利用しない。** 決定が覆った場合は元のIDに `Superseded by D-xxx` を残し、新しいIDを振る。

## ドメイン上の重要な制約

以下は直感に反するため、知らないと自信を持って間違える。

### 予測対象はG1だが、学習はG1でやらない（`D-003`）

G1は20年遡っても約7,700出走行しかなく、学習データとして成立しない。
JRA全レース（約90万行）で学習し、クラス特徴量・sample_weight・G1での確率校正で特化させる。
**「G1に絞る」という指示をデータフィルタとして実装しないこと。**

### 予測対象レースのオッズを特徴量にしてはならない（`D-002`）

オッズを入れると精度は上がるが、市場を超える余地が消える。目標は `normalize(1/単勝オッズ)` で定義される**市場確率のLogLossを下回ること**であり、オッズは最終段の期待値計算でのみ使う。

例外は `F-701`/`F-703` のみ。騎手・厩舎の実力を馬質との交絡から切り離すために、**過去レースの**人気を使うことは許可されている（`Q-006`）。この線引きを勝手に広げない。

### モデルが2段階なのはリーク回避のため（`D-007`）

Stage 1（レース質予測）が存在する理由は、**対象レースの実測ラップが発走後にしか判明しない**ため。
実測ラップはStage 1の目的変数としてのみ使い、特徴量に混入させれば即リークになる。

### 同日レースの扱いに例外規定がある

過去成績の集計は `race_date < target_race_date` で厳密にフィルタするのが原則。
ただし `F-501`/`F-502`（当日馬場バイアス）だけは例外で、**対象レースより前に発走した同日のレース**を使ってよい。G1がメインレース（10R〜11R）であることを利用しており、期待値が最も高いと見込んでいる領域。

「後に発走したレース」は当然使えない。この前後判定を実装で間違えないこと。

リーク防止の全6原則は `docs/domain-knowledge.md` 5節にある。`P-1` でテストコード（`tests/test_leakage.py`）として実装し、CIで常時検証する予定。

### バックテストの数字を額面通りに報告しない（`D-008`）

検証に使えるG1は約240レース。単勝の1点あたり標準偏差は σ≈4 程度で、回収率の標準誤差は `4/√240 ≈ 26%`。

**回収率74%〜126%の範囲では有意差を主張できない。** 「バックテストで回収率120%出た」は単なるノイズである可能性が高い。回収率を報告する際は必ずブートストラップ信頼区間を添える。

主評価券種は分散の小さい**複勝・ワイド**。3連単は控除率27.5%かつ超高分散で検出力が絶望的に低いため対象外。

### 一般的な競馬予想の知見がG1では反転する

`docs/domain-knowledge.md` 3節。G1は定量・馬齢重量が中心なので**斤量の情報量が低い**（ハンデ戦と真逆）。前走着順も前哨戦が叩き台になるため結びつきが弱い。ハンデ戦・条件戦を前提にした一般的な特徴量設計をそのまま持ち込まないこと。

## 参照順

初めてこのリポジトリに触れる場合は `docs/README.md` から。
特徴量を実装するときは `docs/domain-knowledge.md` の `F-xxx` を、全体像を掴むときは `docs/architecture.md` を見る。
